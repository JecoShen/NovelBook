import { readFile } from 'node:fs/promises'
import { fileURLToPath } from 'node:url'
import { describe, expect, it } from 'vitest'
import 'nbook/test/source-contract-matcher'

const activityBarPath = fileURLToPath(new URL('../components/novel-ide/NovelIdeActivityBar.vue', import.meta.url))
const indexPagePath = fileURLToPath(new URL('../pages/index.vue', import.meta.url))
const previewPagePath = fileURLToPath(new URL('../pages/world-engine.preview.vue', import.meta.url))
const workbenchPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineWorkbenchDialog.vue', import.meta.url))
const workbenchPreviewInspectorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewInspector.vue', import.meta.url))
const workbenchPreviewMutationEditorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewMutationEditor.vue', import.meta.url))
const workbenchPreviewSidebarPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewSidebar.vue', import.meta.url))
const workbenchPreviewSliceCardPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewSliceCard.vue', import.meta.url))
const workbenchPreviewSliceListPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewSliceList.vue', import.meta.url))
const workbenchPreviewTypesPath = fileURLToPath(new URL('../components/novel-ide/world-engine/workbench-preview/world-engine-workbench-preview.types.ts', import.meta.url))
const themeVarsCssPath = fileURLToPath(new URL('../styles/theme-vars.css', import.meta.url))
const previewProjectPanelPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEnginePreviewProjectPanel.vue', import.meta.url))
const previewActionsPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEnginePreviewActions.vue', import.meta.url))
const previewMutationBuilderPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEnginePreviewMutationBuilder.vue', import.meta.url))
const previewStatePanelPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEnginePreviewStatePanel.vue', import.meta.url))
const mutationEditorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineMutationEditor.vue', import.meta.url))
const mutationEditorHeaderPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineMutationEditorHeader.vue', import.meta.url))
const sliceDraftFormPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineSliceDraftForm.vue', import.meta.url))
const mutationBuilderPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineMutationBuilder.vue', import.meta.url))
const mutationActionButtonsPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineMutationActionButtons.vue', import.meta.url))
const mutationListControlsPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineMutationListControls.vue', import.meta.url))
const objectValueEditorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineObjectValueEditor.vue', import.meta.url))
const subjectCreatorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineSubjectCreator.vue', import.meta.url))
const sliceInspectorPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineSliceInspector.vue', import.meta.url))
const stateSummaryPath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineStateSummary.vue', import.meta.url))
const timelinePath = fileURLToPath(new URL('../components/novel-ide/world-engine/WorldEngineTimeline.vue', import.meta.url))
const realWorkbenchUtilPath = fileURLToPath(new URL('./world-engine-workbench-real.ts', import.meta.url))

function removedToken(...parts: string[]): string {
  return parts.join('')
}

async function readSource(path: string): Promise<string> {
  return (await readFile(path, 'utf-8')).replace(/\r\n/g, '\n')
}

describe('World Engine IDE entry', () => {
  it('保留 Activity Bar 入口并打开当前 Project 的工作台', async () => {
    const activityBar = await readSource(activityBarPath)
    const indexPage = await readSource(indexPagePath)
    const previewPage = await readSource(previewPagePath)
    const workbench = await readSource(workbenchPath)
    const workbenchPreviewInspector = await readSource(workbenchPreviewInspectorPath)
    const workbenchPreviewMutationEditor = await readSource(workbenchPreviewMutationEditorPath)
    const workbenchPreviewSidebar = await readSource(workbenchPreviewSidebarPath)
    const workbenchPreviewSliceCard = await readSource(workbenchPreviewSliceCardPath)
    const workbenchPreviewSliceList = await readSource(workbenchPreviewSliceListPath)
    const workbenchPreviewTypes = await readSource(workbenchPreviewTypesPath)
    const previewProjectPanel = await readSource(previewProjectPanelPath)
    const previewActions = await readSource(previewActionsPath)
    const previewMutationBuilder = await readSource(previewMutationBuilderPath)
    const previewStatePanel = await readSource(previewStatePanelPath)
    const mutationEditor = await readSource(mutationEditorPath)
    const mutationEditorHeader = await readSource(mutationEditorHeaderPath)
    const sliceDraftForm = await readSource(sliceDraftFormPath)
    const mutationBuilder = await readSource(mutationBuilderPath)
    const mutationActionButtons = await readSource(mutationActionButtonsPath)
    const mutationListControls = await readSource(mutationListControlsPath)
    const objectValueEditor = await readSource(objectValueEditorPath)
    const subjectCreator = await readSource(subjectCreatorPath)
    const sliceInspector = await readSource(sliceInspectorPath)
    const stateSummary = await readSource(stateSummaryPath)
    const timeline = await readSource(timelinePath)
    const realWorkbenchUtil = await readSource(realWorkbenchUtilPath)

    expect(activityBar).toContainSource('case \'world\':')
    expect(activityBar).toContainSource('emit(\'open-world-engine\')')
    expect(activityBar).toContainSource('ide.header.worldEngine')
    expect(activityBar).toContainSource('i-lucide-globe-2')
    expect(indexPage).toContainSource('WorldEngineWorkbenchDialog')
    expect(indexPage).toContainSource('openWorldEngineWorkbench')
    expect(indexPage).toContainSource(':project-root="currentProjectRoot"')
    expect(indexPage).toContainSource('const worldEngineWorkbenchHasUnsavedDrafts = ref(false)')
    expect(indexPage).toContainSource('const worldEngineWorkbenchSaving = ref(false)')
    expect(indexPage).toContainSource('if (!worldEngineWorkbenchOpen.value) {')
    expect(indexPage).toContainSource('if (worldEngineWorkbenchSaving.value) {')
    expect(indexPage).toContainSource('World Engine 正在保存 Slice，请等待保存完成后再切换 Project。')
    expect(indexPage).toContainSource('if (!worldEngineWorkbenchHasUnsavedDrafts.value) {')
    expect(indexPage).toContainSource('World Engine Workbench 有未保存草稿。切换 Project 会放弃这些会话草稿。')
    expect(indexPage).toContainSource('放弃草稿并切换')
    expect(indexPage).toContainSource('const confirmWorldEngineWorkbenchDraftDiscardForProjectSwitch = async (): Promise<boolean>')
    expect(indexPage).toContainSource('return action === \'discard\'')
    expect(indexPage).not.toContainSource('worldEngineWorkbenchOpen.value = false\n    worldEngineWorkbenchHasUnsavedDrafts.value = false\n    return true')
    expect(indexPage).toContainSource('const restoreCurrentWorkspaceRoute = async (): Promise<void>')
    expect(indexPage).toContainSource('await router.replace(buildProjectRoute(USER_ASSETS_PROJECT_TARGET))')
    expect(indexPage).toContainSource('if (!(await confirmWorldEngineWorkbenchDraftDiscardForProjectSwitch()))')
    expect(indexPage).toContainSource('await restoreCurrentWorkspaceRoute()')
    expect(indexPage).toContainSource('if (decision === \'cancel\')')
    expect(indexPage).toContainSource('@has-unsaved-drafts-change="worldEngineWorkbenchHasUnsavedDrafts = $event"')
    expect(indexPage).toContainSource('@saving-change="worldEngineWorkbenchSaving = $event"')
    expect(indexPage).toContainSource('@open-workspace-path="void openWelcomeWorkspacePath($event)"')
    expect(previewPage).toContainSource('previewAttrValueType')
    expect(previewPage).toContainSource('resolvePreviewAttrPath')
    expect(previewPage).toContainSource('selectPreviewProjectRoot')
    expect(previewPage).toContainSource('defaultMutationForPreviewSubject')
    expect(previewPage).toContainSource('function applyDefaultSliceMutation')
    expect(previewPage).toContainSource('let lastAutoSliceMutationDraft')
    expect(previewPage).toContainSource('const knownSubjectIds = new Set(subjects.value.map(subject => subject.id))')
    expect(previewPage).toContainSource('if (!currentSubjectExists) {')
    expect(previewPage).toContainSource('function shouldRefreshDefaultSliceMutation(): boolean')
    expect(previewPage).toContainSource('schema.value?.subjectTypes ?? []')
    expect(previewPage).not.toContainSource('JSON.stringify([{subjectId, attr: "events", op: "listAppend", value: "世界引擎初始化"}]')
    expect(previewPage).toContainSource('defaultValueForPreviewAttr')
    expect(previewPage).toContainSource('mutationBuilderValueHint')
    expect(previewPage).toContainSource('const mutationLoadIndex = ref(\'0\')')
    expect(previewPage).toContainSource('const mutationLoadOptions = computed(() => {')
    expect(previewPage).toContainSource('const canUseSelectedMutation = computed(() => mutationLoadOptions.value.length > 0)')
    expect(previewPage).toContainSource('${attr.kind}<${valueType}>')
    expect(previewPage).toContainSource('WorldStateQueryDto')
    expect(previewPage).toContainSource('CreateSubjectResultDto')
    expect(previewPage).toContainSource('stateIssues')
    expect(previewPage).toContainSource('actionIssues')
    expect(previewPage).toContainSource('function setPreviewError(message: string): void')
    expect(previewPage).toContainSource('function setPreviewNotice(message: string): void')
    expect(previewPage).toContainSource('function formatPreviewProjectTitleTimestamp(date: Date): string')
    expect(previewPage).toContainSource('pad(date.getSeconds())')
    expect(previewPage).toContainSource('function defaultPreviewProjectTitle(date = new Date()): string')
    expect(previewPage).toContainSource('function resetCreateProjectForm(): void')
    expect(previewPage).toContainSource('title: defaultPreviewProjectTitle()')
    expect(previewPage).not.toContainSource('previewDemoSchemaError')
    expect(previewPage).not.toContainSource('canSeedDemoWorld')
    expect(previewPage).not.toContainSource('demoWorldButtonTitle')
    expect(previewPage).not.toContainSource('validatePreviewDemoSchema')
    expect(previewPage).toContainSource('setPreviewError(\'subject id 不能为空\')')
    expect(previewPage).toContainSource('setPreviewError(\'subject type 不能为空\')')
    expect(previewPage).toContainSource('setPreviewError(\'subject time 不能为空\')')
    expect(previewPage).toContainSource('setPreviewError(`subject ${subjectId} 已存在，请填写新的 id`)')
    expect(previewPage).toContainSource('subjectForm.id = \'\'')
    expect(previewPage).toContainSource('subjectForm.name = \'\'')
    expect(previewPage).toContainSource('subjectForm.type = subjectType')
    expect(previewPage).toContainSource('subjectForm.time = subjectTime')
    expect(previewPage).toContainSource('queryForm.subjectIds = result.subjectId')
    expect(previewPage).toContainSource('mutationBuilder.subjectId = result.subjectId')
    expect(previewPage).toContainSource('async function loadSubjectIntoQuery(subject: WorldSubjectDto): Promise<void>')
    expect(previewPage).toContainSource('queryForm.type = \'\'')
    expect(previewPage).toContainSource('if (!editingSliceId.value && shouldRefreshDefaultSliceMutation()) {')
    expect(previewPage).toContainSource('applyDefaultSliceMutation(subject.id)')
    expect(previewPage.indexOf('applyDefaultSliceMutation(subject.id)')).toBeGreaterThan(previewPage.indexOf('subjectForm.name = subject.name'))
    expect(previewPage.indexOf('await queryState({ clearActionIssues: false })', previewPage.indexOf('async function loadSubjectIntoQuery'))).toBeGreaterThan(previewPage.indexOf('applyDefaultSliceMutation(subject.id)'))
    expect(previewPage).toContainSource('await queryState({ clearActionIssues: false })')
    expect(previewPage).toContainSource('@load-subject="void loadSubjectIntoQuery($event)"')
    expect(previewPage).toContainSource('const currentBuilderSubject = subjects.value.find(subject => subject.id === mutationBuilder.subjectId)')
    expect(previewPage).toContainSource('if (currentBuilderSubject?.type === typeName) {')
    expect(previewPage).toContainSource('const currentQuerySubjectId = parseCsvList(queryForm.subjectIds)[0] ?? \'\'')
    expect(previewPage).toContainSource('if (currentQuerySubject?.type === typeName) {')
    expect(previewPage.indexOf('await loadWorld()', previewPage.indexOf('subjectForm.id = \'\''))).toBeGreaterThan(previewPage.indexOf('mutationBuilder.subjectId = result.subjectId'))
    expect(previewPage.indexOf('advanceSliceFormTime()', previewPage.indexOf('subjectForm.id = \'\''))).toBeGreaterThan(previewPage.indexOf('await loadWorld()', previewPage.indexOf('subjectForm.id = \'\'')))
    expect(previewPage).toContainSource('stateResult.value = []')
    expect(previewPage).toContainSource('function advanceSliceFormTime(): void')
    expect(previewPage).toContainSource('sliceForm.time = suggestNextPreviewTime(examples, usedTimes)')
    expect(previewPage).toContainSource('function loadSliceForEdit(sliceId: string): void')
    expect(previewPage).toContainSource('loadMutationToBuilder(0, false)')
    expect(previewPage.indexOf('loadMutationToBuilder(0, false)')).toBeGreaterThan(previewPage.indexOf('sliceForm.mutations = JSON.stringify(slice.mutations ?? [], null, 2)'))
    expect(previewPage).toContainSource('if (!sliceForm.time.trim()) {')
    expect(previewPage).toContainSource('setPreviewError(\'time 不能为空\')')
    expect(previewPage).toContainSource('setPreviewError(parsed.message)')
    expect(previewPage).toContainSource('setPreviewError(\'查询必须提供 subjectIds 或 type\')')
    expect(previewPage).toContainSource('setPreviewNotice(result.issues.length')
    expect(previewPage).toContainSource('if (editing) {\n      clearSliceEditMode()\n    }\n    else {\n      advanceSliceFormTime()\n    }')
    expect(previewPage).toContainSource('applyDefaultSliceMutation(mutationBuilder.subjectId || subjectForm.id || \'world\')')
    expect(previewPage.indexOf('await queryState({ clearActionIssues: false })')).toBeGreaterThan(previewPage.indexOf('advanceSliceFormTime()'))
    expect(previewPage).toContainSource('formatWorldEngineConflictMessage(resolveApiErrorMessage(writeError')
    expect(previewPage).toContainSource('const previewProjectListLimit = 80')
    expect(previewPage).toContainSource('typeof route.query.project === \'string\' ? route.query.project : \'\'')
    // 列表接口不接受裁剪参数：预览页消费真实项目列表并在客户端限制展示数量。
    expect(previewPage).toContainSource('const allProjects = (await $fetch<ProjectListResponseDto>(\'/api/projects\')).projects')
    expect(previewPage).toContainSource('projects.value = allProjects.slice(0, previewProjectListLimit)')
    expect(previewPage).not.toContainSource('previewProjectTestPrefixes')
    expect(mutationEditor).toContainSource('formatWorldEngineConflictMessage(resolveApiErrorMessage(error')
    expect(subjectCreator).toContainSource('formatWorldEngineConflictMessage(resolveApiErrorMessage(error')
    // 页面只接线创建、事实刷新和结果展示；失败交错由 utility 行为测试约束。
    expect(previewPage).toContainSource('runPreviewProjectCreate')
    expect(previewPage).toContainSource('refreshPreviewProjectCreate')
    expect(previewPage).toContainSource('applyPreviewCreateSettlement')
    expect(previewPage).toContainSource('request: () => $fetch<ProjectCreateResponseDto>')
    expect(previewPage).toContainSource('refresh: loadProjects')
    expect(previewPage).toContainSource('classifyCommit: createError => resolveProjectMutationCommitState(createError, \'create\')')
    expect(previewPage).toContainSource('if (result.status === \'rejected\')')
    expect(previewPage).toContainSource('if (result.status === \'refresh_failed\')')
    expect(previewPage).toContainSource('result.preferredProjectRoot ? { preferredProjectRoot: result.preferredProjectRoot } : {}')
    expect(previewPage).toContainSource('if (createRecovery.value) return')
    expect(previewPage).toContainSource('async function activatePreviewProject(projectRoot: string): Promise<boolean>')
    expect(previewPage).toContainSource('async function loadProjects(preferredProjectRoot?: string): Promise<Readonly<{')
    expect(previewPage).not.toContainSource('propagateError')
    expect(previewPage).toContainSource('let suppressProjectSelectionWatcher = false')
    expect(previewPage).toContainSource('const nextProjectRoot = selectPreviewProjectRoot(projects.value, preferredProjectRoot, routeProjectRoot, selectedProjectRoot.value)')
    expect(previewPage).toContainSource('if (selectedProjectRoot.value !== nextProjectRoot) {')
    expect(previewPage).toContainSource('suppressProjectSelectionWatcher = true')
    expect(previewPage).toContainSource('selectedProjectRoot.value = nextProjectRoot')
    expect(previewPage).toContainSource('suppressProjectSelectionWatcher = false')
    expect(previewPage).toContainSource([
      'function resetPreviewProjectSessionState(): void {',
      '  lastWriteResult.value = null',
      '  editingSliceId.value = \'\'',
      '  mutationLoadIndex.value = \'0\'',
      '  stateResult.value = []',
      '  stateIssues.value = []',
      '  actionIssues.value = []',
      '  notice.value = \'\'',
      '  error.value = \'\'',
      '}',
    ].join('\n'))
    expect(previewPage).toContainSource([
      'watch(selectedProjectRoot, (projectRoot) => {',
      '  if (suppressProjectSelectionWatcher) {',
      '    return',
      '  }',
      '  void activatePreviewProject(projectRoot)',
      '}, { flush: \'sync\' })',
    ].join('\n'))
    expect(previewPage).toContainSource('await projectSession.release()')
    expect(previewPage).toContainSource('await projectSession.open(projectRoot)')
    expect(previewPage).toContainSource('type WorldLoadRequest = {')
    expect(previewPage).toContainSource('const key = `${projectRoot}:${readyRevision}`')
    expect(previewPage).toContainSource('if (worldLoadRequest?.key === key) return worldLoadRequest.promise')
    expect(previewPage).toContainSource('const loaded = await loadWorld(projectRoot, ready.revision)')
    expect(previewPage).toContainSource('watch(projectSession.state, (next, previous) => {')
    expect(previewPage).toContainSource('clearPreviewProjectData();')
    expect(previewPage).toContainSource('const loaded = await loadWorld(next.ready.projectRoot, next.ready.revision);')
    expect(previewPage).toContainSource('if (revision !== projectSelectionRevision || selectedProjectRoot.value !== projectRoot) return false;')
    expect(previewPage).toContainSource('if (loaded) return true;')
    expect(previewPage).toContainSource('projectSession.state.value.status === "ready"')
    expect(previewPage).toContainSource('deleteSlice')
    expect(previewPage).toContainSource('返回 ${result.issues.length} 个 issue')
    expect(previewPage).toContainSource('import {useDialog} from "nbook/app/composables/useDialog";')
    expect(previewPage).toContainSource('const {confirm: confirmDialog} = useDialog();')
    expect(previewPage).toContainSource('!await confirmDialog(`确定要删除 slice')
    expect(previewPage).not.toContainSource('window.confirm(`确定要删除 slice')
    expect(previewPage).toContainSource('WorldEnginePreviewProjectPanel')
    expect(previewPage).toContainSource('WorldEnginePreviewActions')
    expect(previewPage).toContainSource('WorldEnginePreviewStatePanel')
    expect(previewPage).toContainSource('updateMutationBuilderField')
    expect(previewPage).toContainSource('function loadMutationToBuilder(index: number')
    expect(previewPage).toContainSource('function replaceSelectedBuilderMutation(): void')
    expect(previewPage).toContainSource('function insertAfterSelectedBuilderMutation(): void')
    expect(previewPage).toContainSource('function duplicateSelectedBuilderMutation(): void')
    expect(previewPage).toContainSource('function deleteSelectedBuilderMutation(): void')
    expect(previewPage).toContainSource('function moveSelectedBuilderMutation(direction: "up" | "down"): void')
    expect(previewPage).toContainSource('mutationLoadIndex.value = String(clampMutationIndex(length, index));')
    expect(previewProjectPanel).toContainSource('新建 Project')
    expect(previewProjectPanel).not.toContainSource('创建示例世界')
    expect(previewProjectPanel).toContainSource('loadingProjects: boolean;')
    expect(previewProjectPanel).toContainSource('loadingWorld: boolean;')
    expect(previewPage).toContainSource(':loading-projects="loadingProjects"')
    expect(previewPage).toContainSource(':loading-world="loadingWorld"')
    expect(previewProjectPanel).toContainSource('<fieldset class="space-y-3 disabled:opacity-60" :disabled="loadingProjects || loadingWorld || actionBusy || Boolean(createRecovery)">')
    expect(previewProjectPanel).toContainSource('@click="emit(\'retry-create-recovery\')"')
    expect(previewProjectPanel).not.toContainSource('canSeedDemoWorld')
    expect(previewProjectPanel).not.toContainSource('demoWorldButtonTitle')
    expect(previewProjectPanel).toContainSource('fill-mutation')
    expect(previewProjectPanel).toContainSource(':disabled="loadingProjects || loadingWorld || actionBusy"')
    expect(previewPage).toContainSource('async function refreshProjects(): Promise<void> {\n    if (loadingWorld.value) return;')
    expect(previewPage).toContainSource('async function createProject(): Promise<void> {\n    if (loadingProjects.value) return;\n    if (loadingWorld.value) return;')
    expect(previewPage).toContainSource('const previewBuilderDisabled = computed(() => loadingWorld.value || actionBusy.value);')
    expect(previewPage).toContainSource('function fillMutation(typeName: string, attr: WorldPreviewSchemaAttr): void {\n    if (loadingProjects.value) return;\n    if (previewBuilderDisabled.value) return;')
    expect(previewPage).toContainSource('function addBuilderMutation(mode: "append" | "replace"): void {\n    if (previewBuilderDisabled.value) return;')
    expect(previewPage).toContainSource('function loadMutationToBuilder(index: number, showNotice = true): void {\n    if (previewBuilderDisabled.value) return;')
    expect(previewPage).toContainSource('function updateMutationBuilderField(field: "subjectId" | "path" | "op" | "value", value: string): void')
    expect(previewPage).toContainSource('if (previewBuilderDisabled.value) return;')
    expect(previewPage).toContainSource('function updateMutationLoadIndex(value: string): void {\n    if (previewBuilderDisabled.value) return;')
    expect(previewPage).toContainSource('function requestClearSliceEditMode(): void {\n    if (previewBuilderDisabled.value) return;')
    expect(previewProjectPanel).toContainSource('schema.calendar.examples.join')
    expect(previewProjectPanel).toContainSource('const schemaSourcePath = "world-engine/schema/index.ts";')
    expect(previewProjectPanel).toContainSource('const calendarSourcePath = "world-engine/calendar.ts";')
    expect(previewProjectPanel).toContainSource('function buildIdeOpenPathHref(path: string): string')
    expect(previewProjectPanel).toContainSource('new URLSearchParams({project: props.selectedProject.projectRoot, openPath: path})')
    expect(previewProjectPanel).toContainSource('{{ schemaSourcePath }}')
    expect(previewProjectPanel).toContainSource('{{ calendarSourcePath }}')
    expect(previewProjectPanel.indexOf('v-if="selectedProject"')).toBeLessThan(previewProjectPanel.indexOf('v-if="!schema"'))
    expect(indexPage).toContainSource('async function consumeWorkspaceOpenPathFromRoute(): Promise<void>')
    expect(indexPage).toContainSource('if (filePath === "world-engine/calendar.ts")')
    expect(indexPage).toContainSource('const created = await ensureWorldEngineCalendarFile();')
    expect(indexPage).toContainSource('const nodes = await novelIdeStore.loadWorkspaceTree();')
    expect(indexPage).toContainSource('nodes.some(node => node.path === \'world-engine/calendar.ts\')')
    expect(indexPage).toContainSource('await createMissingWorldEngineCalendarFile();')
    expect(indexPage).toContainSource('function buildWorldEngineCalendarTemplate(): string')
    expect(indexPage).toContainSource('Calendar 配置已就绪')
    expect(indexPage).toContainSource('await openWelcomeWorkspacePath(filePath);')
    expect(indexPage).toContainSource('watch(() => [route.query.project, route.query.openPath] as const')
    expect(indexPage).toContainSource('if (workspaceRouteSynced())')
    expect(indexPage).toContainSource('await consumeWorkspaceOpenPathFromRoute();')
    expect(indexPage).toContainSource('await normalizeNovelRouteQuery();')
    expect(indexPage).toContainSource('await initializeWorkspaceFromRoute(target, revision);')
    expect(indexPage).toContainSource('while (processedProjectRouteRevision < projectRouteIntentRevision)')
    expect(indexPage).toContainSource('await projectSession.open(target.projectRoot);')
    expect(indexPage).toContainSource('// ProjectSession 是存在性真相源；Catalog 仅补充展示 metadata，不能阻塞 direct-open。')
    expect(indexPage).toContainSource('void loadProjects().catch(() => undefined);')
    expect(indexPage).not.toContainSource('const list = await loadProjects();')
    expect(indexPage).not.toContainSource('list.some((novel) => novel.projectRoot === target.projectRoot)')
    expect(indexPage).not.toContainSource('notifyMissingProjectRoute(target.projectRoot)')
    expect(indexPage).toContainSource('await switchToNovelWorkspace(target.projectRoot);')
    expect(indexPage).toContainSource('const projectSurfaceActive = computed(() =>')
    expect(indexPage).toContainSource('projectSession.state.value.ready.projectRoot === currentProjectRoot.value')
    expect(indexPage).toContainSource('v-if="projectSurfaceActive"')
    expect(previewActions).toContainSource('WorldEnginePreviewMutationBuilder')
    expect(previewActions).toContainSource(':disabled="loadingWorld || actionBusy"')
    expect(previewPage).toContainSource('async function refreshProjects(): Promise<void>')
    expect(previewPage).toContainSource('if (actionBusy.value) return;\n    try {\n        await loadProjects();')
    expect(previewPage).toContainSource('void refreshProjects();')
    expect(previewPage).toContainSource(':disabled="loadingProjects || loadingWorld || actionBusy"')
    expect(previewPage).toContainSource('@click="void refreshProjects()"')
    expect(previewActions).toContainSource('const subjectIdAlreadyExists = computed(() => {')
    expect(previewActions).toContainSource('props.subjects.some(subject => subject.id === subjectId)')
    expect(previewActions).toContainSource('loadingWorld: boolean;')
    expect(previewPage).toContainSource(':loading-world="loadingWorld"')
    expect(previewActions).toContainSource('const canCreateSubject = computed(() => props.projectReady && !props.loadingWorld && !props.actionBusy && subjectForm.value.id.trim() && subjectForm.value.type.trim() && subjectForm.value.time.trim() && !subjectIdAlreadyExists.value)')
    expect(previewPage).toContainSource('async function createSubject(): Promise<void> {\n    if (loadingWorld.value) return;')
    expect(previewActions).toContainSource('该 subject 已存在。点击左侧 subject 会载入查询上下文；新建 subject 请填写新的 id。')
    expect(previewActions).toContainSource(':disabled="!canCreateSubject"')
    expect(previewActions).toContainSource('const canWriteSlice = computed(() => props.projectReady && !props.loadingWorld && !props.actionBusy && sliceForm.value.time.trim())')
    expect(previewPage).toContainSource('async function writeSlice(): Promise<void> {\n    if (loadingWorld.value) return;')
    expect(previewPage).toContainSource('async function refreshWorldFromStatePanel(): Promise<void> {\n    if (loadingWorld.value) return;\n    if (actionBusy.value) return;')
    expect(previewPage).toContainSource('async function deleteSlice(sliceId: string): Promise<void> {\n    if (loadingWorld.value) return;')
    expect(previewPage).toContainSource('@refresh="void refreshWorldFromStatePanel()"')
    expect(previewActions).toContainSource(':disabled="!canWriteSlice"')
    expect(previewActions).toContainSource('const canQueryState = computed(() => props.projectReady && !props.loadingWorld && !props.actionBusy && (queryForm.value.subjectIds.trim() || queryForm.value.type.trim()))')
    expect(previewPage).toContainSource('async function queryState(options: {clearActionIssues?: boolean} = {}): Promise<void> {\n    if (loadingWorld.value && options.clearActionIssues !== false) return;')
    expect(previewActions).toContainSource(':disabled="!canQueryState"')
    expect(previewActions).toContainSource('Create Subject')
    expect(previewActions).toContainSource('Write Slice')
    expect(previewActions).toContainSource('<fieldset class="space-y-2 disabled:opacity-60" :disabled="loadingWorld || actionBusy">')
    expect(previewActions.match(/class="space-y-2 disabled:opacity-60"/g)?.length).toBe(3)
    expect(previewActions).toContainSource('Query')
    expect(previewActions).toContainSource('write-slice')
    expect(previewActions).toContainSource('query-state')
    expect(previewActions).toContainSource(':state-result="stateResult"')
    expect(previewActions).toContainSource(':mutation-load-options="mutationLoadOptions"')
    expect(previewActions).toContainSource(':mutation-load-index="mutationLoadIndex"')
    expect(previewActions).toContainSource(':can-use-selected-mutation="canUseSelectedMutation"')
    expect(previewPage).toContainSource('@update-mutation-load-index="updateMutationLoadIndex"')
    expect(previewPage).toContainSource('@clear-slice-edit-mode="requestClearSliceEditMode"')
    expect(previewActions).toContainSource('insert-after-selected-mutation')
    expect(previewActions).toContainSource('duplicate-selected-mutation')
    expect(previewActions).toContainSource('replace-selected-mutation')
    expect(previewActions).toContainSource('delete-selected-mutation')
    expect(previewActions).toContainSource('move-selected-mutation')
    expect(previewActions).not.toContainSource('Resettle')
    expect(previewActions).not.toContainSource(removedToken('rese', 'ttle', '-timeline'))
    expect(previewMutationBuilder).toContainSource('Mutation Builder')
    expect(previewMutationBuilder).toContainSource('WorldEngineMutationListControls')
    expect(previewMutationBuilder).toContainSource('WorldEngineMutationActionButtons')
    expect(previewMutationBuilder).toContainSource('valueHint')
    expect(previewMutationBuilder).toContainSource('subjectTypeLabel')
    expect(previewMutationBuilder).toContainSource('mutationLoadOptions')
    expect(previewMutationBuilder).toContainSource('canUseSelectedMutation')
    expect(previewMutationBuilder).not.toContainSource('collectionRemoveValueOptions')
    expect(previewMutationBuilder).not.toContainSource('syncCollectionRemoveValue')
    expect(previewMutationBuilder).not.toContainSource('从当前 State Query 结果中选择要移除的 collection 项')
    expect(previewMutationBuilder).toContainSource('JSON Pointer path, e.g. /memory/师门')
    expect(previewMutationBuilder).toContainSource('update-builder-field')
    expect(previewMutationBuilder).toContainSource('add-builder-mutation')
    expect(previewMutationBuilder).toContainSource('builder.op === \'remove\'')
    expect(mutationActionButtons).toContainSource('i-lucide-list-plus')
    expect(mutationActionButtons).toContainSource('i-lucide-refresh-ccw')
    expect(previewStatePanel).toContainSource('World State')
    expect(previewStatePanel).toContainSource('State Query')
    expect(previewStatePanel).toContainSource('formatSlicePatches')
    expect(previewStatePanel).toContainSource('actionBusy: boolean;')
    expect(previewPage).toContainSource(':action-busy="actionBusy"')
    expect(previewStatePanel).toContainSource(':disabled="loadingWorld || actionBusy || !projectReady"')
    expect(previewStatePanel).toContainSource('load-subject')
    expect(previewStatePanel).toContainSource('load-slice')
    expect(previewStatePanel).toContainSource('delete-slice')
    expect(previewStatePanel).toContainSource('stateIssues')
    expect(previewStatePanel).toContainSource('本次操作 issues')
    expect(previewStatePanel).toContainSource('i-lucide-trash-2')
    expect(previewStatePanel).toContainSource('aria-label="载入编辑 slice"')
    expect(previewStatePanel).toContainSource('aria-label="删除 slice"')
    expect(previewStatePanel).toContainSource('编辑')
    expect(previewStatePanel).toContainSource('删除')
    expect(previewStatePanel).toContainSource('refresh')
    expect(workbench).toContainSource('WorldEngineWorkbenchPreviewSidebar')
    expect(workbench).toContainSource(':busy="workbenchActionBusy"')
    expect(workbench).toContainSource(':focused-subject-id="focusedSubjectId"')
    expect(workbench).toContainSource('@clear-subject-context="clearSubjectContext"')
    expect(workbench).toContainSource('function clearSubjectContext(): void')
    expect(workbench).toContainSource('notification.success("已清空主体文件建议语境。");')
    expect(workbench).toContainSource('(e: "openWorkspacePath", path: string): void;')
    expect(workbench).toContainSource('function openWorkspacePathFromWorkbench(path: string): Promise<void>')
    expect(workbench).toContainSource('打开工作区文件会关闭 Workbench 并放弃这些会话草稿')
    expect(workbench).toContainSource('const {confirm: confirmDialog} = useDialog();')
    expect(workbench).toContainSource('confirmDialog(`确定要删除 slice')
    expect(workbench).toContainSource('emit("update:modelValue", false);')
    expect(workbench).toContainSource('await nextTick();')
    expect(workbench).toContainSource('emit("openWorkspacePath", targetPath);')
    expect(workbench).toContainSource('@open-workspace-path="void openWorkspacePathFromWorkbench($event)"')
    expect(workbench).toContainSource('WorldEngineWorkbenchPreviewSliceList')
    expect(workbench).toContainSource('WorldEngineWorkbenchPreviewMutationEditor')
    expect(workbench).toContainSource('const workbenchActionBusy = computed(() => loading.value || timelineLoading.value || actionBusy.value || sliceComposerSaving.value);')
    expect(workbench).toContainSource('function blockSliceComposerSaving(message = "Slice Composer 正在保存，请稍候再切换工作台上下文。"): boolean')
    expect(workbench).toContainSource('function blockWorkbenchActionBusy(message = "World Engine 工作台正在同步，请稍候再操作。"): boolean')
    expect(workbench).toContainSource('if (blockSliceComposerSaving("Slice Composer 正在保存，请稍候再删除 Slice。")) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再删除 Slice。")) {')
    expect(workbench).toContainSource('if (blockSliceComposerSaving("Slice Composer 正在保存，请稍候再同步主体系统。")) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再同步主体系统。")) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再新建 Slice。")) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再编辑其它 Slice。")) {')
    expect(workbench).toContainSource('if (blockSliceComposerSaving()) {\n        return;\n    }\n    if (blockWorkbenchActionBusy()) {\n        return;\n    }\n    selectedSliceId.value = sliceId;')
    expect(workbench).toContainSource('async function updateSelectedSubjectIdsForTimeline(subjectIds: string[]): Promise<void> {\n    if (blockSliceComposerSaving()) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再切换 subject 过滤。")) {')
    expect(workbench).toContainSource('function updateSliceSearchForTimeline(value: string): void {\n    if (blockSliceComposerSaving()) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再搜索时间线。")) {')
    expect(workbench).toContainSource('function updateSliceKindFilterForTimeline(filter: string): void {\n    if (blockSliceComposerSaving()) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再切换 kind 过滤。")) {')
    expect(workbench).toContainSource('function updateSliceHealthFilterForTimeline(filter: WorldWorkbenchPreviewSliceHealthFilter): void {\n    if (blockSliceComposerSaving()) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再切换状态过滤。")) {')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再查看草稿。")) {')
    expect(workbench).toContainSource(':busy="workbenchActionBusy"')
    expect(workbench).toContainSource('data-testid="world-workbench-draft-summary"')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy"')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy"')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy || !schema"')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy || !schema || !selectedSlice"')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy || !selectedSlice"')
    expect(workbench).toContainSource('WorldEngineWorkbenchPreviewInspector')
    expect(workbench).toContainSource('WorldEngineMutationEditor')
    expect(workbench).toContainSource('world-slice-composer')
    expect(workbench).toContainSource('新建 Slice')
    expect(workbench).toContainSource('handleSliceComposerSaved')
    expect(workbench).toContainSource('continueAfterSave: boolean')
    expect(workbench).toContainSource('const continueSuffix = payload.continueAfterSave ? "，已准备下一步草稿" : "";')
    expect(workbench).toContainSource('sliceComposerVisible.value = payload.continueAfterSave;')
    expect(workbench).toContainSource('const sliceComposerSaving = ref(false);')
    expect(workbench).toContainSource('sliceComposerSaving.value || snapshotLoading.value')
    expect(workbench).toContainSource('if (!payload.continueAfterSave) {')
    expect(workbench).toContainSource('sliceComposerEditorKey.value += 1;')
    expect(workbench).toContainSource('contextSubjectId: string')
    expect(workbench).toContainSource('const continueSubjectId = payload.continueAfterSave ? payload.contextSubjectId : "";')
    expect(workbench).toContainSource('focusedSubjectId.value = continueSubjectId;')
    expect(workbench).toContainSource('mutations: WorldSlicePatchDto[]')
    expect(workbench).toContainSource('clearSubjectFilterIfSavedSliceWouldBeHidden(payload.mutations);')
    expect(workbench).toContainSource('function clearSubjectFilterIfSavedSliceWouldBeHidden(mutations: WorldSlicePatchDto[]): void')
    expect(workbench).toContainSource('selectedSubjectIds.value = [];')
    expect(workbench).toContainSource('function updateSliceComposerSaving(saving: boolean): void')
    expect(workbench).toContainSource('@saving-change="updateSliceComposerSaving"')
    expect(workbench).toContainSource('@input.capture="markSliceComposerDirtyFromInput"')
    expect(workbench).toContainSource('@saved="void handleSliceComposerSaved($event)"')
    expect(workbench).toContainSource('world-inspector-restore-rail')
    expect(workbench).toContainSource('function toggleInspectorPanel(): void')
    expect(workbench).toContainSource('openInspectorPanel(selectedSliceSubjectFileProposalCount.value ? "subject-file-proposals" : undefined);')
    expect(workbench).toContainSource('@click="toggleInspectorPanel"')
    expect(workbench).toContainSource('selectedSliceSubjectFileProposalCount')
    expect(workbench).toContainSource('inspectorButtonAttentionClass')
    expect(workbench).toContainSource('data-testid="world-workbench-inspector-proposal-count"')
    expect(workbench).toContainSource('data-testid="world-inspector-restore-proposal-count"')
    expect(workbench).toContainSource('个主体文件建议')
    expect(workbench).toContainSource(':subject-system-summaries="subjectSystemSummaries"')
    expect(workbench).toContainSource('WorldEngineSubjectCreator')
    expect(workbench).toContainSource('const subjectCreatorOpen = ref(false);')
    expect(workbench).toContainSource('function openSubjectCreatorPanel(): void')
    expect(workbench).toContainSource('sidebarCollapsed.value = false;')
    expect(workbench).toContainSource('subjectCreatorOpen.value = true;')
    expect(workbench).toContainSource('function updateSubjectCreatorOpen(event: Event): void')
    expect(workbench).toContainSource(':open="subjectCreatorOpen"')
    expect(workbench).toContainSource('@toggle="updateSubjectCreatorOpen"')
    expect(workbench).toContainSource('@click="openSubjectCreatorPanel"')
    expect(workbench).toContainSource('isWorldWorkbenchSubjectSystemMaintenanceSlice')
    expect(workbench).toContainSource('normalizeWorldWorkbenchSlices')
    expect(workbench).toContainSource('buildWorldWorkbenchEditSliceBody')
    expect(workbench).toContainSource('buildWorldWorkbenchReviewQueueItems')
    expect(workbench).toContainSource('/api/projects/world-engine/schema')
    expect(workbench).toContainSource('/api/projects/world-engine/subjects')
    expect(workbench).toContainSource('/api/projects/world-engine/slices')
    expect(workbench).toContainSource('/api/projects/rag/overview')
    expect(workbench).toContainSource('limit: sliceLimit')
    expect(workbench).toContainSource('withPatches: "true"')
    expect(workbench).toContainSource('refreshWorldForCurrentTimeline')
    expect(workbench).toContainSource('await loadWorld({...options, preferredSliceId, preferredSubjectIds});')
    expect(workbench).toContainSource('!selectedSubjectIds.value.some((subjectId) => worldSubjectIdSet.value.has(subjectId))')
    expect(workbench).toContainSource('autoSelectSlice: options.autoSelectSlice')
    expect(workbench).toContainSource('reloadTimelineForCurrentSubjectFilter')
    expect(workbench).toContainSource('sliceSubjectFilterQuery')
    expect(workbench).toContainSource('subjectIds: registeredSubjectIds.join(",")')
    expect(workbench).toContainSource('subjectMode: subjectFilterMode.value')
    expect(realWorkbenchUtil).toContainSource('const modeLabel = input.subjectFilterMode === "all" ? "全部 subject" : "任一 subject";')
    expect(workbench).toContainSource('@update:selected-subject-ids="void updateSelectedSubjectIdsForTimeline($event)"')
    expect(workbench).toContainSource('@update-subject-filter-mode="void updateSubjectFilterModeForTimeline($event)"')
    expect(workbench).toContainSource('@update-slice-health-filter="updateSliceHealthFilterForTimeline"')
    expect(workbench).toContainSource('@update-slice-kind-filter="updateSliceKindFilterForTimeline"')
    expect(workbench).toContainSource('@update-slice-search="updateSliceSearchForTimeline"')
    expect(workbench).toContainSource('/api/projects/world-engine/state/query')
    expect(workbench).toContainSource('/api/projects/world-engine/state')
    expect(workbench).toContainSource('/edit')
    expect(workbench).not.toContainSource(removedToken('/api/projects/world-engine/rese', 'ttle'))
    expect(workbench).toContainSource('/world-engine.preview?')
    expect(workbench).toContainSource('full-snapshot-mode')
    expect(workbench).toContainSource('loadFullSnapshot')
    expect(workbench).toContainSource('loadSelectedSliceSnapshots')
    expect(workbench).toContainSource('const snapshotIssues = ref<WorldIssueDto[]>([]);')
    expect(workbench).toContainSource('const fullSnapshotIssues = ref<WorldIssueDto[] | null>(null);')
    expect(workbench).toContainSource('const snapshotSlice = slice.previousTime === undefined || !slice.mutations.length ? await loadSliceDetailForSnapshot(slice) : slice;')
    expect(workbench).toContainSource('function loadSliceDetailForSnapshot(slice: WorldWorkbenchPreviewSlice): Promise<WorldWorkbenchPreviewSlice>')
    expect(workbench).toContainSource('`/api/projects/world-engine/slices/${encodeURIComponent(slice.id)}`')
    expect(workbench).toContainSource('const existingIndex = slices.value.findIndex((item) => item.id === loadedSlice.id);')
    expect(workbench).toContainSource('index === existingIndex ? loadedSlice : item')
    expect(workbench).toContainSource(': mergeWorldWorkbenchTimelineSlice(slices.value, loadedSlice);')
    expect(workbench).toContainSource('const previousSnapshotTime = snapshotSlice.previousTime ?? "";')
    expect(workbench).not.toContainSource('const previousSnapshotTime = slice.previousTime ?? previousSlice?.time ?? "";')
    expect(workbench).toContainSource('previousSnapshotTime ? querySubjectsAt(subjectIds, previousSnapshotTime)')
    expect(workbench).toContainSource('snapshotIssues.value = currentResult.issues;')
    expect(workbench).toContainSource('fullSnapshotIssues.value = result.issues;')
    expect(workbench).toContainSource(':snapshot-issues="snapshotIssues"')
    expect(workbench).toContainSource(':full-snapshot-issues="fullSnapshotIssues"')
    expect(workbenchPreviewInspector).toContainSource('snapshotIssues?: WorldIssueDto[]')
    expect(workbenchPreviewInspector).toContainSource('fullSnapshotIssues?: WorldIssueDto[] | null')
    expect(workbenchPreviewInspector).toContainSource('busy?: boolean;')
    expect(workbenchPreviewInspector).toContainSource('visibleSnapshotIssues')
    expect(workbenchPreviewInspector).toContainSource('data-testid="snapshot-query-issues"')
    expect(workbenchPreviewInspector).toContainSource('State issues ·')
    expect(workbenchPreviewInspector).toContainSource('grid-cols-[112px_minmax(0,1fr)]')
    expect(workbenchPreviewInspector).toContainSource(':title="issue.code"')
    expect(workbenchPreviewInspector).toContainSource('issues: visibleSnapshotIssues.value')
    expect(workbenchPreviewInspector).toContainSource('metadataPatchMatchesSlice')
    expect(workbenchPreviewInspector).toContainSource('外部 slice 成功同步到新值后再自动清理草稿')
    expect(workbenchPreviewInspector).toContainSource('if (props.busy || !metadataDraftDirty.value) {')
    expect(workbenchPreviewInspector).toContainSource('if (props.busy || props.fullSnapshotLoading) {')
    expect(workbenchPreviewInspector).toContainSource('function requestFullSnapshotIfNeeded(): void')
    expect(workbenchPreviewInspector).toContainSource('!props.fullSnapshotLoading && !props.busy')
    expect(workbenchPreviewInspector).toContainSource('props.fullSnapshotSubjects?.length ?? 0, props.busy] as const, requestFullSnapshotIfNeeded')
    expect(workbenchPreviewInspector).toContainSource('<FormInput v-model="draft.time" :disabled="props.busy" />')
    expect(workbenchPreviewInspector).toContainSource('<FormSelect v-model="draft.kind" :options="kindOptions" :disabled="props.busy" />')
    expect(workbenchPreviewInspector).toContainSource('<FormTextarea v-model="draft.summary" :rows="4" :disabled="props.busy" />')
    expect(workbenchPreviewInspector).toContainSource(':disabled="props.busy || props.fullSnapshotLoading"')
    expect(workbenchPreviewInspector).toContainSource(':disabled="props.busy || !metadataDraftDirty"')
    expect(workbenchPreviewInspector).not.toContainSource('delete metadataDrafts[props.slice.id];\n    emit("applyPatch"')
    expect(workbench).toContainSource('saveMetadataPatch')
    expect(workbench).toContainSource('saveMutationValuePatches')
    expect(workbench).toContainSource('buildWorkbenchPreviewFiltersAfterSavedEdit')
    expect(workbench).toContainSource('clearFiltersIfSavedEditWouldBeHidden')
    expect(workbench).toContainSource('const nextFilters = buildWorkbenchPreviewFiltersAfterSavedEdit')
    expect(workbench).toContainSource('sliceHealthFilter.value = nextFilters.sliceHealthFilter;')
    expect(workbench).toContainSource('handleSubjectCreated')
    expect(workbench).toContainSource('preferredSubjectIds')
    expect(workbench).toContainSource('latestSliceTouchingSubjects')
    expect(workbench).toContainSource('keepEmptyPreferredSubjectView')
    expect(workbench).toContainSource('focusedSubjectId.value = preferredSubjectIds.at(-1) ?? "";')
    expect(workbench).toContainSource('emptySliceState')
    expect(workbench).not.toContainSource('demoWorldSchemaError')
    expect(workbench).not.toContainSource('canSeedDemoWorld')
    expect(workbench).not.toContainSource('demoWorldButtonTitle')
    expect(workbench).toContainSource('const canCreateWorldSubject = computed(() => hasWorldSchemaType.value && !hasWorldSubject.value);')
    expect(workbench).toContainSource('async function createWorldSubject(): Promise<void>')
    expect(workbench).toContainSource('const preservedSubjectContextId = focusedSubjectId.value && hasSubjectSystemSummary(focusedSubjectId.value)')
    expect(workbench).toContainSource('const nextSelectedSubjectIds = preservedSubjectContextId ? selectedBeforeCreate : ["world"];')
    expect(workbench).toContainSource('focusedSubjectId.value = nextFocusedSubjectId;')
    expect(workbench).not.toContainSource('data-testid="world-subject-bootstrap-panel"')
    expect(workbench).not.toContainSource('当前 schema 支持 world.events，但 Project 还没有 world subject。')
    expect(workbench).not.toContainSource('不会写入 simulation/subjects 六文件')
    expect(workbench).toContainSource('buildWorldWorkbenchEmptySliceState')
    expect(workbench).toContainSource('const emptySliceState = computed<WorldWorkbenchEmptySliceState>(() => buildWorldWorkbenchEmptySliceState({')
    expect(realWorkbenchUtil).toContainSource('export type WorldWorkbenchEmptySliceAction')
    expect(realWorkbenchUtil).toContainSource('export function buildWorldWorkbenchEmptySliceState')
    expect(workbench).toContainSource('emptySliceState.action === \'create-subject\'')
    expect(workbench).toContainSource('emptySliceState.action === \'create-world-subject\'')
    expect(workbench).toContainSource('@click="void createWorldSubject()"')
    expect(workbench).not.toContainSource(':disabled="workbenchActionBusy || !canSeedDemoWorld"')
    expect(workbench).not.toContainSource(':title="demoWorldButtonTitle"')
    expect(workbench).toContainSource('const emptyReviewQueueItems = computed(() => reviewQueueItems.value.slice(0, 3));')
    expect(workbench).toContainSource('data-testid="empty-slice-review-issues"')
    expect(workbench).toContainSource('{{ reviewTriageSummary.open }}/{{ reviewTriageSummary.total }}')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy"')
    expect(workbench).toContainSource('@click="void focusReviewIssue(item)"')
    expect(workbench).toContainSource('emptySliceState.action === \'sync-subject-system\'')
    expect(workbench).toContainSource(':disabled="workbenchActionBusy || !subjectSystemSyncTime"')
    expect(workbench).toContainSource('@click="void syncPendingSubjectSystemSubjects()"')
    expect(realWorkbenchUtil).toContainSource('同步不会复制或改写 simulation/subjects 六文件正文')
    expect(realWorkbenchUtil).toContainSource('同步只注册身份，不复制或改写六文件正文')
    expect(workbench).toContainSource('buildWorldWorkbenchSubjectSystemInitialAttrs')
    expect(workbench).toContainSource('attrs: declaredSubjectSystemInitialAttrs(summary, subjectType)')
    expect(workbench).toContainSource('function declaredSubjectSystemInitialAttrs(summary: WorldWorkbenchPreviewSubjectSystemSummary, subjectType: string): Record<string, WorkbenchJsonValue>')
    expect(workbench).toContainSource('if (attrNames.has(attr))')
    expect(realWorkbenchUtil).toContainSource('当前 subject 时间线暂无 slice')
    expect(realWorkbenchUtil).toContainSource('当前未选择 slice')
    expect(workbench).toContainSource('emptySliceState.action === \'new-slice\'')
    expect(workbench).toContainSource('清空 subject 过滤')
    expect(workbench).toContainSource('await refreshWorldForCurrentTimeline({preferredSubjectIds: [payload.subject.id]});')
    expect(workbench).toContainSource('await refreshWorldForCurrentTimeline({preferredSubjectIds: created});')
    expect(workbench).toContainSource('deleteSelectedSlice')
    expect(workbench).toContainSource('const nextDraftSliceId = firstRemainingDraftSliceId(slice.id);')
    expect(workbench).toContainSource('clearSessionStateForDeletedSlice(slice.id);')
    expect(workbench).toContainSource('enterDraftViewForSlice(nextDraftSliceId);')
    expect(workbench).toContainSource('await refreshWorldForCurrentTimeline({autoSelectSlice: false, preferredSliceId: nextDraftSliceId || undefined});')
    expect(workbench).toContainSource('recordTransientIssues(result.issues, slice.id, slice);')
    expect(workbench).toContainSource('function firstRemainingDraftSliceId(deletedSliceId: string): string')
    expect(workbench).toContainSource('function enterDraftViewForSlice(sliceId: string): void')
    expect(workbench).toContainSource('function clearSessionStateForDeletedSlice(sliceId: string): void')
    expect(workbench).toContainSource('discardSessionDraftsForSlice(sliceId);')
    expect(workbench).toContainSource('function discardSessionDraftsForSlice(sliceId: string): void')
    expect(workbench).toContainSource('draftDiscardSliceId.value = sliceId;')
    expect(workbench).toContainSource('draftDiscardVersion.value += 1;')
    expect(workbench).toContainSource('metadataDraftSummaries.value = metadataDraftSummaries.value.filter((draft) => draft.sliceId !== sliceId);')
    expect(workbench).toContainSource('valueDraftSummaries.value = valueDraftSummaries.value.filter((draft) => draft.sliceId !== sliceId);')
    expect(workbench).toContainSource('highlightedMutationFocus.value = null;')
    expect(workbench).toContainSource(':discard-draft-slice-id="draftDiscardSliceId"')
    expect(workbench).toContainSource(':discard-draft-version="draftDiscardVersion"')
    expect(workbench).toContainSource('function recordTransientIssues(issues: WorldIssueDto[], fallbackSliceId: string, fallbackSlice?: Pick<WorldWorkbenchPreviewSlice, "id" | "time" | "title">): void')
    expect(workbench).toContainSource('?? (fallbackSlice?.id === sliceId ? fallbackSlice : null)')
    expect(workbench).toContainSource('?? (selectedSlice.value?.id === sliceId ? selectedSlice.value : null)')
    expect(workbench).toContainSource('loadSliceIntoTimeline')
    expect(workbench).toContainSource('const targetSlice = await loadSliceIntoTimeline(item.sliceId);')
    expect(workbench).toContainSource('`/api/projects/world-engine/slices/${encodeURIComponent(sliceId)}`')
    expect(workbench).toContainSource('mergeWorldWorkbenchTimelineSlice')
    expect(workbench).toContainSource('slices.value = mergeWorldWorkbenchTimelineSlice(slices.value, loadedSlice);')
    expect(workbench).toContainSource('const draftSliceIds = computed(() => collectDraftSliceIds());')
    expect(workbench).toContainSource('function collectDraftSliceIds(): string[]')
    expect(workbench).toContainSource('const targetDraftSliceIds = draftSliceIds.value;')
    expect(workbench).toContainSource('await loadSliceIntoTimeline(sliceId);')
    expect(workbench).toContainSource('const firstDraftSliceId = targetDraftSliceIds.find((sliceId) => slices.value.some((slice) => slice.id === sliceId));')
    expect(workbench).toContainSource('Issue 所属 slice ${item.sliceId || "(未知)"} 当前未加载')
    expect(workbench.indexOf('highlightedMutationFocus.value = null', workbench.indexOf('Issue 所属 slice ${item.sliceId || \'(未知)\'} 当前未加载'))).toBeGreaterThan(workbench.indexOf('Issue 所属 slice ${item.sliceId || \'(未知)\'} 当前未加载'))
    expect(workbench).toContainSource('buildWorldWorkbenchCurrentReviewQueueIndex')
    expect(realWorkbenchUtil).toContainSource('export function buildWorldWorkbenchCurrentReviewQueueIndex')
    expect(realWorkbenchUtil).toContainSource('item.key === focus.issueKey')
    expect(workbench).toContainSource('issueKey: item.key')
    expect(workbench).toContainSource('function clearMissingReviewIssueFocus(): void')
    expect(workbench).toContainSource('shouldClearWorldWorkbenchReviewIssueFocus')
    expect(realWorkbenchUtil).toContainSource('export function shouldClearWorldWorkbenchReviewIssueFocus')
    expect(realWorkbenchUtil).toContainSource('const issueKey = input.focus?.issueKey ?? "";')
    expect(realWorkbenchUtil).toContainSource('!input.reviewQueueItems.some((item) => item.key === issueKey)')
    expect(workbench).toContainSource('watch(() => reviewQueueItems.value.map((item) => item.key).join("\\u0000"), clearMissingReviewIssueFocus);')
    expect(workbench).toContainSource('sliceSearch.value = "";')
    expect(workbench).toContainSource('setSubjectFilterForReviewIssue(item.subjectId);')
    expect(workbench).toContainSource('await reloadTimelineForCurrentSubjectFilter({preferredSliceId: item.sliceId, preferredSubjectIds: [item.subjectId]});')
    expect(workbench).toContainSource('if (!slices.value.some((slice) => slice.id === targetSlice.id)) {')
    expect(workbench).toContainSource('let preserveMutationFocusOnSubjectFilterChange = false;')
    expect(workbench).toContainSource('preserveMutationFocusOnSubjectFilterChange = true;')
    expect(workbench).toContainSource('if (preserveMutationFocusOnSubjectFilterChange) {')
    expect(workbench).toContainSource('preserveMutationFocusOnSubjectFilterChange = false;')
    expect(workbench).toContainSource([
      'watch(() => selectedSubjectIds.value.join("\\u0000"), () => {',
      '    if (!selectedSubjectIds.value.length) {',
      '        highlightedMutationFocus.value = null;',
      '        return;',
      '    }',
    ].join('\n'))
    expect(workbenchPreviewInspector).toContainSource('visibleSnapshotIssues')
    expect(workbenchPreviewInspector).toContainSource('discardDraftSliceId?: string;')
    expect(workbenchPreviewInspector).toContainSource('discardMetadataDraftForSlice')
    expect(workbenchPreviewTypes).toContainSource('issueKey?: string')
    expect(workbenchPreviewMutationEditor).toContainSource('currentSliceReviewItems.value.find((item) => item.key === focus.issueKey)')
    expect(workbenchPreviewMutationEditor).toContainSource('busy?: boolean;')
    expect(workbenchPreviewMutationEditor).toContainSource('discardDraftSliceId?: string;')
    expect(workbenchPreviewMutationEditor).toContainSource('discardValueDraftsForSlice')
    expect(workbenchPreviewMutationEditor).toContainSource('requestResetAllValueDrafts')
    expect(workbenchPreviewMutationEditor).toContainSource('if (props.busy) {\n        return;\n    }\n    const key = valueDraftKey(index);')
    expect(workbenchPreviewMutationEditor).toContainSource('function focusReviewIssue(item: WorldWorkbenchPreviewReviewQueueItem | null): void {\n    if (props.busy || !item) {')
    expect(workbenchPreviewMutationEditor).toContainSource('function navigateSubjectSlice(direction: "previous" | "next"): void {\n    if (props.busy) {')
    expect(workbenchPreviewMutationEditor).toContainSource('function navigateToOtherDraft(): void {\n    if (props.busy || !nextOtherSliceDraft.value) {')
    expect(workbenchPreviewMutationEditor).toContainSource('const valueDraftIdentities = reactive<Record<string, string>>({});')
    expect(workbenchPreviewMutationEditor).toContainSource('function mutationDraftIdentity(mutation: WorldSlicePatchDto): string')
    expect(workbenchPreviewMutationEditor).toContainSource('valueDraftIdentities[key] !== mutationDraftIdentity(mutation)')
    expect(workbenchPreviewMutationEditor).toContainSource('Reflect.deleteProperty(valueDraftIdentities, key)')
    expect(workbenchPreviewMutationEditor).toContainSource('props.slice.mutations.map(mutationDraftIdentity).join')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !dirtyValueDraftCount"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy" :title="`跳到 ${nextOtherSliceDraft.sliceTitle} 的未应用草稿`"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !previousReviewQueueItem"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !nextReviewQueueItem"')
    expect(workbenchPreviewMutationEditor).toContainSource('data-testid="mutation-editor-issue-row"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !previousRelatedSlice"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !nextRelatedSlice"')
    expect(workbenchPreviewMutationEditor).toContainSource(':disabled="props.busy || !isValueDraftDirty(row.index, row.mutation)"')
    expect(workbench).toContainSource('function setWorkbenchError(message: string): void')
    expect(workbench).toContainSource('function setWorkbenchNotice(message: string): void')
    expect(workbench).toContainSource('setWorkbenchNotice(message);')
    expect(workbench).toContainSource('notice.value = "";')
    expect(workbench).toContainSource('error.value = "";')
    expect(workbench).toContainSource('@error="setWorkbenchError"')
    expect(workbench).toContainSource('@notice="setWorkbenchNotice"')
    expect(workbench).not.toContainSource('@error="error = $event"')
    expect(workbench).not.toContainSource('@notice="notice = $event"')
    expect(workbench).toContainSource('sliceComposerVisible')
    expect(workbench).toContainSource('sliceComposerDirty')
    expect(workbench).toContainSource('if (!sliceComposerVisible.value) {')
    expect(workbench).toContainSource('    sliceComposerDirty.value = false;')
    expect(workbench).toContainSource('sliceComposerEditorKey')
    expect(workbench).toContainSource('sliceComposerUsedTimes')
    expect(workbench).toContainSource('const knownSliceTimes = ref<string[]>([]);')
    expect(workbench).toContainSource('const sliceComposerUsedTimes = computed(() => [...new Set([...knownSliceTimes.value, ...sliceTimesFromSlices(slices.value)])]);')
    expect(workbench).toContainSource('function replaceKnownSliceTimes(sourceSlices: WorldWorkbenchPreviewSlice[]): void')
    expect(workbench).toContainSource('function mergeKnownSliceTimes(sourceSlices: WorldWorkbenchPreviewSlice[]): void')
    expect(workbench).toContainSource('return collectWorldWorkbenchSliceTimes(sourceSlices);')
    expect(workbench).toContainSource('knownSliceTimes.value = mergeWorldWorkbenchKnownSliceTimes(knownSliceTimes.value, sourceSlices);')
    expect(workbench).toContainSource('replaceKnownSliceTimes(normalizedSlices);')
    expect(workbench).toContainSource('mergeKnownSliceTimes(normalizedSlices);')
    expect(workbench).toContainSource('mergeKnownSliceTimes([loadedSlice]);')
    expect(workbench).toContainSource(':used-times="sliceComposerUsedTimes"')
    expect(workbench).toContainSource('openSelectedSliceComposer')
    expect(workbench).toContainSource('await nextTick();')
    expect(workbench).toContainSource('sliceComposerLoadKey.value += 1;')
    expect(workbench).toContainSource('编辑 Slice')
    expect(workbench).toContainSource('删除 Slice')
    expect(workbench).toContainSource('void deleteSelectedSlice()')
    expect(workbench).toContainSource('i-lucide-trash-2')
    expect(workbench).toContainSource(':key="sliceComposerEditorKey"')
    expect(workbench).toContainSource(':load-slice-key="sliceComposerLoadKey"')
    expect(workbench).toContainSource('const sliceComposerNewKey = ref(0);')
    expect(workbench).toContainSource('const wasVisible = sliceComposerVisible.value;')
    expect(workbench).toContainSource('sliceComposerNewKey.value += 1;')
    expect(workbench).toContainSource(':new-slice-key="sliceComposerNewKey"')
    expect(workbench).toContainSource('@dirty-change="updateSliceComposerDirty"')
    expect(workbench).toContainSource('function sliceComposerHasUnsavedDraft(): boolean')
    expect(workbench).toContainSource('function markSliceComposerDirtyFromInput(): void')
    expect(mutationEditor).toContainSource('loadMutationToBuilder(0, true);')
    expect(workbench).toContainSource('data-testid="world-slice-composer-close"')
    expect(workbench).toContainSource('aria-label="关闭 Slice Composer"')
    expect(workbench).toContainSource('data-testid="world-workbench-close"')
    expect(workbench).toContainSource('aria-label="关闭 World Engine Workbench"')
    expect(workbench).toContainSource('当前 Slice Composer 有未保存草稿，确定关闭吗？')
    expect(workbench).toContainSource('Slice Composer 正在保存，请稍候再关闭。')
    expect(workbench).toContainSource('Slice Composer 正在保存，请稍候再关闭 Workbench。')
    expect(workbench).toContainSource('World Engine 工作台正在同步，请稍候再关闭 Workbench。')
    expect(workbench).toContainSource('World Engine 工作台正在同步，请稍候再打开工作区文件。')
    expect(workbench).toContainSource('World Engine 工作台正在同步，请稍候再打开 Preview。')
    expect(workbench).toContainSource('async function closeSliceComposer(): Promise<void>')
    expect(workbench).toContainSource('async function requestWorkbenchClose(): Promise<void>')
    expect(workbench).toContainSource('function openPreview(): void')
    expect(workbench).toContainSource('if (blockWorkbenchActionBusy("World Engine 工作台正在同步，请稍候再打开 Preview。")) {')
    expect(workbench).toContainSource('async function handleWorkbenchModelUpdate(value: boolean): Promise<void>')
    expect(workbench).toContainSource('await requestWorkbenchClose();')
    expect(workbench).toContainSource('const unsavedLabels = workbenchUnsavedDraftLabels();')
    expect(workbench).toContainSource('!await confirmDialog("当前 Slice Composer 有未保存草稿，确定关闭吗？", "Slice Composer 草稿未保存")')
    expect(workbench).toContainSource('当前 Workbench 有未保存内容：${unsavedLabels.join("、")}。确定关闭并放弃吗？')
    expect(workbench).toContainSource('World Engine 草稿未保存')
    expect(workbench).toContainSource('function workbenchUnsavedDraftLabels(): string[]')
    expect(workbench).toContainSource('buildWorldWorkbenchUnsavedDraftLabels')
    expect(realWorkbenchUtil).toContainSource('export function buildWorldWorkbenchUnsavedDraftLabels')
    expect(realWorkbenchUtil).toContainSource('labels.push("Slice Composer 草稿");')
    expect(realWorkbenchUtil).toContainSource('labels.push(`${input.metadataDraftCount} 个 metadata 草稿`);')
    expect(realWorkbenchUtil).toContainSource('labels.push(`${input.valueDraftSliceCount} 个 value 草稿`);')
    expect(workbench).toContainSource('(e: \'update:modelValue\' | \'hasUnsavedDraftsChange\' | \'savingChange\', value: boolean): void')
    expect(workbench).toContainSource('emit("hasUnsavedDraftsChange", hasUnsavedDrafts);')
    expect(workbench).toContainSource('emit("savingChange", saving);')
    expect(workbench).toContainSource('@update:model-value="void handleWorkbenchModelUpdate($event)"')
    expect(workbench).toContainSource('@request-close="void requestWorkbenchClose()"')
    expect(workbench).toContainSource('if (payload.editing) {\n        discardSessionDraftsForSlice(payload.result.sliceId);\n    }')
    expect(workbench).toContainSource('await refreshWorldForCurrentTimeline({preferredSliceId: payload.result.sliceId});')
    expect(workbench).toContainSource('buildWorldWorkbenchSubjectFileProposals')
    expect(workbench).toContainSource('可在右侧 Inspector 查看 ${proposalCount} 个主体文件建议。')
    expect(workbench).toContainSource('const proposalContextSubjectId = payload.contextSubjectId || focusedSubjectId.value;')
    expect(workbench).toContainSource('focusedSubjectId.value = proposalContextSubjectId;')
    expect(workbench).toContainSource('recordTransientIssues(payload.result.issues, payload.result.sliceId);')
    expect(workbench).toContainSource('pendingSubjectSystemSummaries')
    expect(workbench).toContainSource('subjectSystemSyncTimeOverride')
    expect(workbench).toContainSource('subjectSystemDefaultSyncTime')
    expect(workbench).toContainSource('subjectSystemSyncTime')
    expect(workbench).toContainSource('syncPendingSubjectSystemSubjects')
    expect(workbench).toContainSource('subject-system-sync-panel')
    expect(workbench).toContainSource('初始化时间')
    expect(workbench).toContainSource('v-model.trim="subjectSystemSyncTimeOverride"')
    expect(workbench).toContainSource('aria-label="同步主体系统初始化时间"')
    expect(workbench).toContainSource('if (!created.length) {')
    expect(workbench).toContainSource('但后续同步失败')
    expect(workbench).toContainSource('同步主体系统')
    expect(workbench).toContainSource('const worldSubjectIdSet = computed(() => new Set(worldSubjects.value.map((subject) => subject.id)));')
    expect(workbench).toContainSource('buildWorldWorkbenchSliceComposerSubjectSelection')
    expect(workbench).toContainSource('const sliceComposerRequestedSubjectId = computed(() => sliceComposerSubjectSelection.value.requestedSubjectId);')
    expect(workbench).toContainSource('const sliceComposerSubjectId = computed(() => sliceComposerSubjectSelection.value.subjectId);')
    expect(realWorkbenchUtil).toContainSource('export function buildWorldWorkbenchSliceComposerSubjectSelection')
    expect(realWorkbenchUtil).toContainSource('selectedRegisteredSubjectId ?? input.focusedSubjectId ?? input.selectedSubjectIds.at(-1) ?? ""')
    expect(realWorkbenchUtil).toContainSource('input.worldSubjectIds[0] ?? "world"')
    expect(workbench).toContainSource('主体 ${subjectName} 尚未接入 World Engine。请先同步主体系统')
    expect(workbench).toContainSource('当前 Project 还没有 World Engine subject。请先创建 subject 或同步主体系统。')
    expect(workbench).toContainSource('const pendingSubjectTimelineNoticePrefix = "待接入 subject 暂无 World Engine 时间线";')
    expect(workbench).toContainSource('const pendingSubjectIds = subjectIds.filter((subjectId) => !worldSubjectIdSet.value.has(subjectId));')
    expect(workbench).toContainSource('const registeredSubjectIds = subjectIds.filter((subjectId) => worldSubjectIdSet.value.has(subjectId));')
    expect(workbench).toContainSource([
      'if (!subjectIds.length && subjectFilterMode.value !== "any") {',
      '        subjectFilterMode.value = "any";',
      '    }',
    ].join('\n'))
    expect(workbench).toContainSource([
      'selectedSubjectIds.value = selectedSubjectIds.value.filter((subjectId) => subjectIdSet.has(subjectId));',
      '    if (!selectedSubjectIds.value.length) {',
      '        subjectFilterMode.value = "any";',
      '    }',
    ].join('\n'))
    expect(workbench).toContainSource('const switchedSubjectFilterMode = pendingSubjectIds.length > 0 && subjectFilterMode.value === "all";')
    expect(workbench).toContainSource('subjectFilterMode.value = "any";')
    expect(workbench).toContainSource('包含待接入 subject 时已切回“任一 subject”过滤')
    expect(workbench).toContainSource('if (mode === "all" && pendingSubjectIds.length) {')
    expect(workbench).toContainSource('暂不能使用“全部 subject”过滤')
    expect(workbench).toContainSource('if (mode !== subjectFilterMode.value) {')
    expect(workbench).toContainSource('highlightedMutationFocus.value = null;')
    expect(workbench).toContainSource([
      'sliceHealthFilter.value = "draft";',
      '    selectedSubjectIds.value = [];',
      '    subjectFilterMode.value = "any";',
    ].join('\n'))
    expect(workbench).toContainSource('if (subjectIds.length && !registeredSubjectIds.length) {')
    expect(workbench).toContainSource('focusedSubjectId.value = subjectIds.at(-1) ?? "";')
    expect(workbench).toContainSource('请先使用左侧“同步主体系统”，或选择已注册 subject。')
    expect(workbench).toContainSource('ProjectRagOverviewDto')
    expect(workbench).toContainSource('worldSubjects')
    expect(workbench).toContainSource('mergeWorldWorkbenchSubjectsWithSubjectSystem')
    expect(workbench).toContainSource('buildWorldWorkbenchSubjectSystemSummariesFromRagOverview')
    expect(workbench).toContainSource('subjectSystemOverview')
    expect(workbench).toContainSource(':subject-system-summaries="subjectSystemSummaries"')
    expect(workbench).toContainSource(':teleport-target="false"')
    const themeVarsCss = await readFile(themeVarsCssPath, 'utf8')
    expect(themeVarsCss).toContainSource('.world-engine-workbench-theme')
    expect(themeVarsCss).toContainSource('--we-bg-canvas: var(--bg-main);')
    expect(themeVarsCss).toContainSource('--we-bg-panel: var(--bg-panel);')
    expect(themeVarsCss).toContainSource('--we-bg-muted: var(--bg-subtle);')
    expect(themeVarsCss).toContainSource('--we-bg-data: var(--bg-subtle);')
    expect(themeVarsCss).toContainSource('--we-bg-active: var(--bg-hover);')
    expect(themeVarsCss).toContainSource('--we-border-strong: var(--border-strong);')
    expect(themeVarsCss).toContainSource('--we-accent: var(--accent-main);')
    expect(themeVarsCss).toContainSource('--we-accent-soft: var(--accent-bg);')
    expect(themeVarsCss).toContainSource('--we-accent-border: var(--border-accent);')
    expect(workbench).toContainSource('world-engine-workbench-theme')
    expect(themeVarsCss).toContainSource('--we-info: var(--status-info);')
    expect(themeVarsCss).toContainSource('--we-success: var(--status-success);')
    expect(themeVarsCss).toContainSource('--we-success-soft: var(--status-success-bg);')
    expect(themeVarsCss).toContainSource('--we-warning: var(--status-warning);')
    expect(themeVarsCss).toContainSource('--we-warning-border: var(--status-warning-border);')
    expect(themeVarsCss).toContainSource('--we-danger: var(--status-danger);')
    expect(themeVarsCss).toContainSource('--we-danger-border: var(--status-danger-border);')
    expect(themeVarsCss).toContainSource('--we-code-bg: var(--source-bg);')
    expect(workbench).toContainSource('syncStatusDotClass')
    expect(workbench).toContainSource('bg-[var(--we-success)]')
    expect(workbench).toContainSource('border-[var(--we-danger-border)] bg-[var(--we-danger-soft)]')
    expect(workbench).not.toContainSource('--we-bg-canvas: #f6f8f7')
    expect(workbench).not.toContainSource('--we-bg-panel: #ffffff')
    expect(workbench).not.toContainSource('--we-accent: #078768')
    expect(workbench).not.toContainSource('--we-warning: #b86b00')
    expect(workbench).not.toContainSource('--we-bg-active: var(--bg-active);')
    expect(workbench).not.toContainSource('--we-border-strong: var(--border-color-hover);')
    expect(workbench).not.toContainSource('color-mix(in srgb, #f59e0b 48%')
    expect(workbench).not.toContainSource('color-mix(in srgb, #ef4444 14%')
    expect(workbench).not.toContainSource('--bg-main: var(--we-bg-canvas)')
    expect(workbench).not.toContainSource('--accent-main: var(--we-accent)')
    expect(workbench).not.toContainSource('bg-amber-50 px')
    expect(workbench).not.toContainSource('hover:bg-amber-100')
    expect(workbench).not.toContainSource('bg-white px-1.5')
    expect(workbench).not.toContainSource('bg-rose-500')
    expect(workbench).not.toContainSource('bg-emerald-500')
    expect(workbench).not.toContainSource('border-amber-300')
    expect(workbench).toContainSource('CreateSubjectResultDto')
    expect(workbench).toContainSource('recordTransientIssues(result.issues, selectedSlice.value?.id ?? "");')
    expect(workbench).toContainSource('resetWorkbenchSessionState')
    expect(workbench).toContainSource('selectedSliceId.value = ""')
    expect(workbench).toContainSource('selectedSubjectIds.value = []')
    expect(workbench).toContainSource('issueTriageStates')
    expect(workbench).toContainSource('transientIssues')
    expect(workbench).toContainSource('metadataDraftSummaries')
    expect(workbench).toContainSource('valueDraftSummaries')
    expect(workbench).toContainSource('issues.push(...result.issues);')
    expect(workbench).toContainSource('method: "DELETE"')
    expect(workbench).toContainSource('await refreshWorldForCurrentTimeline({autoSelectSlice: false, preferredSliceId: nextDraftSliceId || undefined});')
    expect(workbench).toContainSource('options.autoSelectSlice === false')
    expect(workbench).toContainSource('@click="void refreshWorldForCurrentTimeline()"')
    expect(workbench).not.toContainSource('?? slices.value[0] ?? null')
    expect(workbenchPreviewSliceList).toContainSource('if (!props.selectedSliceId) {')
    expect(workbench).toContainSource('find((slice) => !isWorldWorkbenchSubjectSystemMaintenanceSlice(slice))')
    expect(workbench).not.toContainSource(removedToken('pending', 'Rese', 'ttle'))
    expect(workbench).not.toContainSource(removedToken('rese', 'ttle', 'PendingTimeline'))
    expect(workbench).not.toContainSource('WorldEngineTimeline')
    expect(workbench).not.toContainSource('WorldEngineStateSummary')
    expect(workbench).not.toContainSource('WorldEngineSliceInspector')
    expect(workbench).not.toContainSource('activeTab')
    expect(workbench).not.toContainSource('localStorage')
    expect(workbench).not.toContainSource('mockWorkbench')
    expect(workbench).not.toContainSource('seedDemoWorld')
    expect(realWorkbenchUtil).toContainSource('normalizeWorldWorkbenchSlices')
    expect(realWorkbenchUtil).toContainSource('buildWorldWorkbenchEditSliceBody')
    expect(realWorkbenchUtil).toContainSource('buildWorldWorkbenchReviewQueueItems')
    expect(realWorkbenchUtil).toContainSource('worldWorkbenchSubjectSystemAttrs')
    expect(realWorkbenchUtil).toContainSource('buildWorldWorkbenchSubjectSystemSummaries')
    expect(realWorkbenchUtil).toContainSource('buildWorldWorkbenchSubjectSystemSummariesFromRagOverview')
    expect(realWorkbenchUtil).toContainSource('buildWorldWorkbenchSubjectFileProposals')
    expect(realWorkbenchUtil).toContainSource('formatWorldWorkbenchSubjectFileProposal')
    expect(realWorkbenchUtil).toContainSource('mergeWorldWorkbenchSubjectsWithSubjectSystem')
    expect(realWorkbenchUtil).toContainSource('pending-world-subject')
    expect(realWorkbenchUtil).toContainSource('simulation-subjects-overview')
    expect(realWorkbenchUtil).toContainSource('subjectFiles')
    expect(realWorkbenchUtil).toContainSource('actorImportPath')
    expect(realWorkbenchUtil).toContainSource('ragIndexSources')
    expect(workbenchPreviewSidebar).toContainSource('subjectSystemSummaries')
    expect(workbenchPreviewSidebar).toContainSource('busy?: boolean;')
    expect(workbenchPreviewSidebar).toContainSource('focusedSubjectId?: string;')
    expect(workbenchPreviewSidebar).toContainSource('(e: \'clearSubjectContext\' | \'toggleCollapsed\'): void')
    expect(workbenchPreviewSidebar).toContainSource('function clearSubjectContext(): void')
    expect(workbenchPreviewSidebar).toContainSource('emit("clearSubjectContext");')
    expect(workbenchPreviewSidebar).toContainSource('清语境')
    expect(workbenchPreviewSidebar).toContainSource('语境中')
    expect(workbenchPreviewSidebar).toContainSource('const activeSubjectContextId = computed(() => props.focusedSubjectId && subjectSystemSummaryMap.value.has(props.focusedSubjectId) ? props.focusedSubjectId : "");')
    expect(workbenchPreviewSidebar).toContainSource(':aria-pressed="activeSubjectContextId === subject.id"')
    expect(workbenchPreviewSidebar).toContainSource('function subjectSystemFilePath')
    expect(workbenchPreviewSidebar).toContainSource('function openSubjectSystemFile')
    expect(workbenchPreviewSidebar).toContainSource('emit("openWorkspacePath", path);')
    expect(workbenchPreviewSidebar).toContainSource('if (props.busy) {\n        return;\n    }\n    const next = selectedSubjectSet.value.has(subjectId)')
    expect(workbenchPreviewSidebar).toContainSource('if (props.busy) {\n        return;\n    }\n    emit("update:selectedSubjectIds", []);')
    expect(workbenchPreviewSidebar).toContainSource(':disabled="props.busy" title="打开 schema 配置文件"')
    expect(workbenchPreviewSidebar).toContainSource(':disabled="props.busy" title="打开 calendar.ts 配置文件"')
    expect(workbenchPreviewSidebar).toContainSource('title="打开 subject.md"')
    expect(workbenchPreviewSidebar).toContainSource('title="打开 events.jsonl"')
    expect(workbenchPreviewSidebar).toContainSource('title="打开 memory.jsonl"')
    expect(workbenchPreviewSidebar).toContainSource('title="打开 state.md"')
    expect(workbenchPreviewSidebar).toContainSource(':disabled="props.busy" @click="clearSubjects"')
    expect(workbenchPreviewSidebar).toContainSource(':disabled="props.busy"')
    expect(workbenchPreviewSidebar).toContainSource('主体系统')
    expect(workbenchPreviewSidebar).toContainSource('待接入')
    expect(workbenchPreviewSidebar).toContainSource('controlledBy')
    expect(workbenchPreviewSidebar).toContainSource('legacyKind')
    expect(workbenchPreviewSidebar).toContainSource('eventCount')
    expect(workbenchPreviewSidebar).toContainSource('memoryCount')
    expect(workbenchPreviewInspector).not.toContainSource('subject-system-summary')
    expect(workbenchPreviewInspector).not.toContainSource('来自 simulation/subjects 的真实主体系统摘要')
    expect(workbenchPreviewInspector).not.toContainSource('path only')
    expect(workbenchPreviewInspector).toContainSource('buildWorldWorkbenchSubjectFileProposals')
    expect(workbenchPreviewInspector).toContainSource('data-testid="subject-file-proposals"')
    expect(workbenchPreviewInspector).toContainSource('data-testid="subject-file-proposal-count"')
    expect(workbenchPreviewInspector).toContainSource('Subject file proposals')
    expect(workbenchPreviewInspector).toContainSource('仅生成建议，不会自动写入 simulation/subjects')
    expect(workbenchPreviewInspector).toContainSource('formatWorldWorkbenchSubjectFileProposal')
    expect(workbenchPreviewInspector).toContainSource('navigator.clipboard.writeText')
    expect(workbenchPreviewInspector).toContainSource('复制主体文件建议')
    expect(workbenchPreviewInspector).toContainSource('function copySubjectFileProposalText(text: string, successMessage: string): Promise<boolean>')
    expect(workbenchPreviewInspector).toContainSource('notification.error("复制失败，请手动选择文本后复制。");')
    expect(workbenchPreviewInspector).toContainSource('function copySubjectFileProposalTextAndOpen(text: string, successMessage: string, path: string): Promise<void>')
    expect(workbenchPreviewInspector).toContainSource('World Engine 工作台正在同步，请稍候再打开目标文件。')
    expect(workbenchPreviewInspector).toContainSource('目标文件路径为空，无法打开。')
    expect(workbenchPreviewInspector).toContainSource('if (copied) {')
    expect(workbenchPreviewInspector).toContainSource('openSubjectFileProposalPath(path);')
    expect(workbenchPreviewInspector).toContainSource('proposal.eventJsonLine')
    expect(workbenchPreviewInspector).toContainSource('proposal.memoryJsonLines')
    expect(workbenchPreviewInspector).toContainSource('committedSubjectEventKeys?: string[];')
    expect(workbenchPreviewInspector).toContainSource('committedSubjectEventKeySet')
    expect(workbenchPreviewInspector).toContainSource('worldWorkbenchSubjectEventProposalKey(proposal)')
    expect(workbenchPreviewInspector).toContainSource('这条 events.jsonl 经历已在当前会话处理。')
    expect(workbenchPreviewInspector).toContainSource('(e: "commitSubjectEventProposal", proposal: WorldWorkbenchSubjectFileProposal): void;')
    expect(workbenchPreviewInspector).toContainSource('function commitSubjectEventProposal(proposal: WorldWorkbenchSubjectFileProposal): void')
    expect(workbenchPreviewInspector).toContainSource('emit("commitSubjectEventProposal", proposal);')
    expect(workbenchPreviewInspector).toContainSource('确认后追加到 events.jsonl')
    expect(workbenchPreviewInspector).toContainSource('已追加')
    expect(workbenchPreviewInspector).toContainSource('title="复制 events.jsonl 行"')
    expect(workbenchPreviewInspector).toContainSource('title="复制 events.jsonl 行并打开文件，确认后追加到文件末尾"')
    expect(workbenchPreviewInspector).toContainSource('title="复制 memory.jsonl 候选行"')
    expect(workbenchPreviewInspector).toContainSource('title="复制 memory.jsonl 候选行并打开文件，确认后追加新行或按 topic 改写"')
    expect(workbenchPreviewInspector).toContainSource('title="复制 state.md 审查提示并打开文件，打开后检查对应区块"')
    expect(workbenchPreviewInspector).toContainSource('复制并打开')
    expect(workbenchPreviewInspector).toContainSource('memory.jsonl 候选行已复制。')
    expect(workbenchPreviewInspector).toContainSource('events.jsonl 行已复制，打开文件后确认并追加到末尾。')
    expect(workbenchPreviewInspector).toContainSource('写入前确认第一人称口吻、角色当时知道什么；确认后追加到 events.jsonl 末尾。')
    expect(workbenchPreviewInspector).toContainSource('memory.jsonl 候选行已复制，打开文件后确认追加新行或按 topic 改写。')
    expect(workbenchPreviewInspector).toContainSource('memory.jsonl 是当前认知快照；写入前确认追加新行，还是按 topic 改写已有行。')
    expect(workbenchPreviewInspector).toContainSource('state.md 审查提示已复制，打开文件后检查对应区块。')
    expect(workbenchPreviewInspector).toContainSource('(e: \'focusSubject\' | \'openWorkspacePath\', payload: string): void')
    expect(workbenchPreviewInspector).toContainSource('function openSubjectFileProposalPath(path: string): void')
    expect(workbenchPreviewInspector).toContainSource('emit("openWorkspacePath", targetPath);')
    expect(workbenchPreviewInspector).toContainSource('title="打开 events.jsonl"')
    expect(workbenchPreviewInspector).toContainSource('title="打开 memory.jsonl"')
    expect(workbenchPreviewInspector).toContainSource('title="打开 state.md"')
    expect(workbenchPreviewInspector).toContainSource('events.jsonl draft')
    expect(workbenchPreviewInspector).toContainSource('memory facts')
    expect(workbenchPreviewInspector).toContainSource('state.md review')
    expect(workbenchPreviewInspector).toContainSource('subjectFileProposalFocusVersion?: number;')
    expect(workbenchPreviewInspector).toContainSource('function scrollSubjectFileProposalsIntoView(): Promise<void>')
    expect(workbenchPreviewInspector).toContainSource('subjectFileProposalsRef.value?.scrollIntoView({block: "start"});')
    expect(workbenchPreviewInspector).toContainSource('watch(() => props.subjectFileProposalFocusVersion')
    expect(workbenchPreviewInspector).toContainSource('ref="subjectFileProposalsRef"')
    expect(workbenchPreviewSliceList).toContainSource('subjectSystemSummaries?: WorldWorkbenchPreviewSubjectSystemSummary[]')
    expect(workbenchPreviewSliceList).toContainSource(':subject-system-summaries="props.subjectSystemSummaries ?? []"')
    expect(workbenchPreviewSliceList).toContainSource('openInspectorPanel: (target?: "subject-file-proposals") => void;')
    expect(workbenchPreviewSliceList).toContainSource('openSubjectFileProposals(sliceId: string, subjectId: string)')
    expect(workbenchPreviewSliceList).toContainSource('emit("focusSubject", subjectId);')
    expect(workbenchPreviewSliceList).toContainSource('props.openInspectorPanel("subject-file-proposals");')
    expect(workbenchPreviewSliceList).toContainSource('@open-subject-file-proposals="openSubjectFileProposals"')
    expect(workbench).toContainSource(':open-inspector-panel="openInspectorPanel"')
    expect(workbench).toContainSource('const subjectFileProposalFocusVersion = ref(0);')
    expect(workbench).toContainSource('function openInspectorPanel(target?: "subject-file-proposals"): void')
    expect(workbench).toContainSource(':subject-file-proposal-focus-version="subjectFileProposalFocusVersion"')
    expect(workbench).toContainSource('SubjectEventCommitResultDto')
    expect(workbench).toContainSource('const committedSubjectEventKeys = ref<string[]>([]);')
    expect(workbench).toContainSource(':committed-subject-event-keys="committedSubjectEventKeys"')
    expect(workbench).toContainSource('function markSubjectEventProposalCommitted(proposal: WorldWorkbenchSubjectFileProposal): void')
    expect(workbench).toContainSource('markSubjectEventProposalCommitted(proposal);')
    expect(workbench).toContainSource('committedSubjectEventKeys.value = [];')
    expect(workbench).toContainSource('function commitSubjectEventProposal(proposal: WorldWorkbenchSubjectFileProposal): Promise<void>')
    expect(workbench).toContainSource('/api/projects/world-engine/subject-file-proposals/events/commit')
    expect(workbench).toContainSource('await refreshSubjectSystemOverview();')
    expect(workbench).toContainSource('@commit-subject-event-proposal="void commitSubjectEventProposal($event)"')
    expect(workbenchPreviewSliceCard).toContainSource('data-testid="slice-card-subject-file-proposal-count"')
    expect(workbenchPreviewSliceCard).toContainSource('@click.stop="openSubjectFileProposals"')
    expect(workbenchPreviewSliceCard).toContainSource('subjectFileProposals.value[0]?.subjectId')
    expect(realWorkbenchUtil).toContainSource('persistedWorldWorkbenchIssueKey')
    expect(realWorkbenchUtil).toContainSource('worldWorkbenchIssueIdentity')
    expect(realWorkbenchUtil).toContainSource('const eventContextMutations = eventContextMutationsForSubject(input.slice.mutations, subjectId, contextMutations);')
    expect(realWorkbenchUtil).toContainSource('const eventText = buildSubjectEventText(input.slice, subjectName, eventContextMutations);')
    expect(realWorkbenchUtil).toContainSource('function eventContextMutationsForSubject')
    expect(realWorkbenchUtil).toContainSource('eventJsonLine: JSON.stringify({text: eventText, time: input.slice.time})')
    expect(realWorkbenchUtil).toContainSource('function buildSubjectMemoryJsonLines')
    expect(realWorkbenchUtil).toContainSource('function buildSubjectEventText')
    expect(realWorkbenchUtil).toContainSource('function subjectVoiceText')
    expect(realWorkbenchUtil).toContainSource('function memoryProposalTopic')
    expect(realWorkbenchUtil).toContainSource('function stateReviewSection')
    expect(realWorkbenchUtil).toContainSource('检查 state.md「${stateReviewSection(mutation.path)}」')
    expect(mutationEditor).toContainSource('parseMutationJson')
    expect(mutationEditor).toContainSource('defaultMutationForPreviewAttr')
    expect(mutationEditor).toContainSource('defaultValueForPreviewAttr')
    expect(mutationEditor).toContainSource('resolvePreviewAttrPath')
    expect(mutationEditor).toContainSource('WorldEngineMutationBuilder')
    expect(mutationEditor).toContainSource('WorldEngineMutationEditorHeader')
    expect(mutationEditor).toContainSource('WorldEngineSliceDraftForm')
    expect(mutationEditor).toContainSource('const initialSubjectId = props.selectedSubjectId || props.subjects[0]?.id || "world";')
    expect(mutationEditor).toContainSource('defaultMutationForPreviewSubject')
    expect(mutationEditor).toContainSource('const initialMutation = defaultMutationForPreviewSubject(props.schema?.subjectTypes ?? [], props.subjects, initialSubjectId);')
    expect(mutationEditor).toContainSource('当前 Project 还没有 ${typeName} subject，不能使用该 schema shortcut。请先创建对应 subject。')
    expect(mutationEditor).toContainSource('return props.subjects.find((subject) => subject.type === typeName)?.id ?? "";')
    expect(mutationEditor).toContainSource(':disabled="schemaTypeShortcutDisabled(type.type)"')
    expect(mutationEditor).toContainSource('usedTimes?: string[];')
    expect(mutationEditor).toContainSource('(e: \'dirtyChange\' | \'savingChange\', value: boolean): void')
    expect(mutationEditor).toContainSource('emit("savingChange", true);')
    expect(mutationEditor).toContainSource('emit("savingChange", false);')
    expect(mutationEditor).toContainSource('if (!props.projectRoot || props.busy || saving.value) {')
    expect(mutationEditor).toContainSource('if (props.busy || saving.value) {\n        return;\n    }\n    if (hasDirtyDraft.value) {')
    expect(mutationEditor).toContainSource('if (props.busy || saving.value) {\n        return;\n    }\n    pendingLoadSelectedSlice.value = false;')
    expect(mutationEditor).toContainSource('import {useDialog} from "nbook/app/composables/useDialog";')
    expect(mutationEditor).toContainSource('const {confirm: confirmDialog} = useDialog();')
    expect(mutationEditor).toContainSource('async function clearEditMode(): Promise<void>')
    expect(mutationEditor).toContainSource('if (props.busy || saving.value) {\n        return;\n    }\n    if (hasDirtyDraft.value && import.meta.client && !await confirmDialog')
    expect(mutationEditor).not.toContainSource('window.confirm("当前编辑器有未保存草稿')
    expect(mutationEditor).toContainSource('newSliceKey: number;')
    expect(mutationEditor).toContainSource('watch(() => props.newSliceKey, (key) => {')
    expect(mutationEditor).toContainSource('void clearEditMode();')
    expect(mutationEditor).toContainSource('const builderDisabled = computed(() => Boolean(props.busy) || saving.value);')
    expect(mutationEditor).toContainSource('function fillMutation(typeName: string, attr: WorldPreviewSchemaAttr): void {\n    if (builderDisabled.value) {')
    expect(mutationEditor).toContainSource('function updateBuilderField(field: "subjectId" | "path" | "op" | "value", value: string): void {\n    if (builderDisabled.value) {')
    expect(mutationEditor).toContainSource('function addBuilderMutation(mode: "append" | "replace"): void {\n    if (builderDisabled.value) {')
    expect(mutationEditor).toContainSource('function loadMutationToBuilder(index: number, silent = false): void {\n    if (builderDisabled.value) {')
    expect(mutationEditor).toContainSource('function updateObjectBuilderRow(index: number, patch: Partial<ObjectBuilderRow>): void {\n    if (builderDisabled.value) {')
    expect(mutationEditor).toContainSource('suggestNextPreviewTime')
    expect(mutationEditor).toContainSource('time: suggestedNewSliceTime()')
    expect(mutationEditor).toContainSource('function suggestedNewSliceTime(extraUsedTimes: string[] = []): string')
    expect(mutationEditor).toContainSource('return suggestNextPreviewTime(examples, usedTimes);')
    expect(mutationEditor).toContainSource('watch(hasDirtyDraft, (dirty) => {')
    expect(mutationEditor).toContainSource('emit("dirtyChange", dirty);')
    expect(mutationEditor).toContainSource('当前编辑器有未保存草稿，确定切换到新建模式吗？')
    expect(mutationEditor).toContainSource('mutations: defaultSliceMutations(initialSubjectId)')
    expect(mutationEditor).toContainSource('subjectId: initialMutation.subjectId')
    expect(mutationEditor).toContainSource('path: initialMutation.path')
    expect(mutationEditor).toContainSource('op: initialMutation.op')
    expect(mutationEditor).toContainSource('function applyDefaultSliceMutation(subjectId: string): void')
    expect(mutationEditor).toContainSource('mutationBuilder.subjectId = mutation.subjectId')
    expect(mutationEditor).toContainSource('applyDefaultSliceMutation(subjectId);')
    expect(mutationEditor).toContainSource('applyDefaultSliceMutation(props.selectedSubjectId || mutationBuilder.subjectId || "world");')
    expect(mutationEditor).toContainSource('continueAfterSave: boolean')
    expect(mutationEditor).toContainSource('contextSubjectId: string')
    expect(mutationEditor).toContainSource('const continueAfterSave = options.continueAfterSave === true && !editing;')
    expect(mutationEditor).toContainSource('sliceForm.time = suggestedNewSliceTime([savedTime]);')
    expect(mutationEditor).toContainSource('const defaultForSelected = selectedSubjectId ? defaultMutationForPreviewSubject(props.schema?.subjectTypes ?? [], props.subjects, selectedSubjectId) : null;')
    expect(mutationEditor).toContainSource('const keepSelectedContext = Boolean(')
    expect(mutationEditor).toContainSource('const contextSubjectId = keepSelectedContext ? selectedSubjectId : lastMutation?.subjectId || selectedSubjectId || mutationBuilder.subjectId || "world";')
    expect(mutationEditor).toContainSource('applyDefaultSliceMutation(contextSubjectId);')
    expect(mutationEditor).toContainSource('const lastContinueSaveNotice = ref("");')
    expect(mutationEditor).toContainSource('上一条已写入 ${result.sliceId}')
    expect(mutationEditor).toContainSource('data-testid="slice-composer-continue-save-notice"')
    expect(mutationEditor).toContainSource('function suggestedNewSliceTime(extraUsedTimes: string[] = []): string')
    expect(mutationEditor).toContainSource('const usedTimes = [...(props.usedTimes ?? []), ...extraUsedTimes];')
    expect(mutationEditor).toContainSource(':show-continue-action="!editingSliceId"')
    expect(mutationEditor).toContainSource(':disabled="builderDisabled"')
    expect(mutationEditor).toContainSource('@submit-and-continue="submitSlice({continueAfterSave: true})"')
    expect(mutationEditor).toContainSource('mutations: WorldSlicePatchDto[]')
    expect(mutationEditor).toContainSource('emit("saved", {result, time: savedTime, editing, continueAfterSave, contextSubjectId, mutations: parsed.value});')
    expect(sliceDraftForm).toContainSource('showContinueAction: boolean;')
    expect(sliceDraftForm).toContainSource('(e: \'submit\' | \'submit-and-continue\'): void')
    expect(sliceDraftForm).toContainSource(':disabled="saving"')
    expect(sliceDraftForm).toContainSource('写入并继续下一步')
    expect(mutationEditor).toContainSource('const wasClean = !hasDirtyDraft.value;')
    expect(mutationEditor).toContainSource('if (wasClean) {')
    expect(mutationEditor).toContainSource('markCleanSliceForm();')
    expect(mutationEditor).toContainSource(':state-result="props.stateResult"')
    expect(mutationEditor).toContainSource('updateBuilderField')
    expect(mutationEditor).toContainSource('updateObjectBuilderRow')
    expect(mutationEditor).toContainSource('addBuilderMutation')
    expect(mutationEditor).toContainSource('insertAfterSelectedBuilderMutation')
    expect(mutationEditor).toContainSource('duplicateSelectedBuilderMutation')
    expect(mutationEditor).toContainSource('insertMutationAfter')
    expect(mutationEditor).toContainSource('duplicateMutationAt')
    expect(mutationEditor).toContainSource('replaceSelectedBuilderMutation')
    expect(mutationEditor).toContainSource('const builderDraftDirty = ref(false);')
    expect(mutationEditor).toContainSource('function applyDirtyBuilderDraftBeforeSubmit(): boolean')
    expect(mutationEditor).toContainSource('if (!applyDirtyBuilderDraftBeforeSubmit()) {')
    expect(mutationEditor).toContainSource('function parseBuilderValue(): ReturnType<typeof parseLooseJsonValue>')
    expect(mutationEditor).toContainSource('builderValueMode.value === "text" || builderValueMode.value === "ref"')
    expect(mutationEditor).toContainSource('deleteSelectedBuilderMutation')
    expect(mutationEditor).toContainSource('moveSelectedBuilderMutation')
    expect(mutationEditor).toContainSource('buildMutationFromBuilder')
    expect(mutationEditor).toContainSource('mutationValidation')
    expect(mutationEditor).toContainSource('const sliceValidation = computed(() => {')
    expect(mutationEditor).toContainSource('time 不能为空')
    expect(mutationEditor).toContainSource('sliceValidation.value.ok')
    expect(mutationEditor).toContainSource(':validation-ok="sliceValidation.ok"')
    expect(mutationEditor).toContainSource('mutationLoadOptions')
    expect(mutationEditor).toContainSource('updateMutationLoadIndex')
    expect(mutationEditor).toContainSource('loadMutationToBuilder')
    expect(mutationEditor).toContainSource('String(next.length - 1)')
    expect(mutationEditor).toContainSource('mutationLoadOptions.value.length')
    expect(mutationBuilder).toContainSource('Mutation Builder')
    expect(mutationBuilder).toContainSource('builderValueHint')
    expect(mutationBuilder).toContainSource('WorldEngineMutationListControls')
    expect(mutationBuilder).toContainSource('WorldEngineMutationActionButtons')
    expect(mutationBuilder).toContainSource('WorldEngineObjectValueEditor')
    expect(mutationBuilder).toContainSource('disabled?: boolean;')
    expect(mutationBuilder).toContainSource('<fieldset class="m-0 border-0 p-0 disabled:opacity-70" :disabled="props.disabled">')
    expect(mutationBuilder).not.toContainSource('collectionValueOptions')
    expect(mutationBuilder).not.toContainSource('collectionRemoveValueOptions')
    expect(mutationBuilder).not.toContainSource('syncCollectionRemoveValue')
    expect(mutationBuilder).not.toContainSource('从当前 State Query 结果中选择要移除的 collection 项')
    expect(mutationBuilder).toContainSource('update-builder-field')
    expect(mutationBuilder).toContainSource('update-object-row')
    expect(mutationBuilder).toContainSource('update-mutation-load-index')
    expect(mutationBuilder).toContainSource('add-mutation')
    expect(mutationBuilder).toContainSource('insert-after-selected-mutation')
    expect(mutationBuilder).toContainSource('duplicate-selected-mutation')
    expect(mutationBuilder).toContainSource('replace-selected-mutation')
    expect(mutationBuilder).toContainSource('delete-selected-mutation')
    expect(mutationBuilder).toContainSource('move-selected-mutation')
    expect(mutationBuilder).toContainSource('load-mutation')
    expect(mutationListControls).toContainSource('mutationLoadOptions')
    expect(mutationListControls).toContainSource('载入')
    expect(mutationListControls).toContainSource('上移所选 mutation')
    expect(mutationListControls).toContainSource('下移所选 mutation')
    expect(mutationListControls).toContainSource('move-selected-mutation')
    expect(mutationListControls).toContainSource('load-mutation')
    expect(mutationActionButtons).toContainSource('canUseSelectedMutation')
    expect(mutationActionButtons).toContainSource('add-mutation')
    expect(mutationActionButtons).toContainSource('insert-after-selected-mutation')
    expect(mutationActionButtons).toContainSource('duplicate-selected-mutation')
    expect(mutationActionButtons).toContainSource('replace-selected-mutation')
    expect(mutationActionButtons).toContainSource('delete-selected-mutation')
    expect(mutationActionButtons).toContainSource('追加')
    expect(mutationActionButtons).toContainSource('插入其后')
    expect(mutationActionButtons).toContainSource('复制所选')
    expect(mutationActionButtons).toContainSource('替换所选')
    expect(mutationActionButtons).toContainSource('删除所选')
    expect(mutationActionButtons).toContainSource('替换全部')
    expect(objectValueEditor).toContainSource('objectBuilderRows')
    expect(objectValueEditor).toContainSource('Object Value')
    expect(objectValueEditor).toContainSource('Object Fields')
    expect(objectValueEditor).toContainSource('row.enabled')
    expect(objectValueEditor).toContainSource('启用字段')
    expect(objectValueEditor).toContainSource('add-object-row')
    expect(objectValueEditor).toContainSource('remove-object-row')
    expect(objectValueEditor).toContainSource('update-object-row')
    expect(objectValueEditor).toContainSource('objectFieldValueMode(row.key) === \'number\'')
    expect(objectValueEditor).toContainSource('objectFieldValueMode(row.key) === \'boolean\'')
    expect(objectValueEditor).toContainSource('objectFieldValueMode(row.key) === \'enum\'')
    expect(objectValueEditor).toContainSource('objectFieldValueMode(row.key) === \'ref\'')
    expect(objectValueEditor).toContainSource('objectFieldValueMode(row.key) === \'json\'')
    expect(objectValueEditor).toContainSource('嵌套 object 字段需要填写 JSON 对象')
    expect(mutationBuilder).toContainSource('builderValueMode === \'number\'')
    expect(mutationBuilder).toContainSource('builderValueMode === \'boolean\'')
    expect(mutationBuilder).toContainSource('builderValueMode === \'enum\'')
    expect(mutationBuilder).toContainSource('builderValueMode === \'ref\'')
    expect(mutationBuilder).toContainSource('builderValueMode === \'object\'')
    expect(mutationBuilder).toContainSource('builderValueMode === \'json\'')
    expect(mutationBuilder).toContainSource('当前 value 需要填写 JSON object')
    expect(mutationBuilder).toContainSource('JSON Pointer path, e.g. /equipment/weapon /memory/师门')
    expect(mutationEditor).toContainSource('/api/projects/world-engine/slices')
    expect(mutationEditor).toContainSource('/edit')
    expect(mutationEditor).toContainSource('hasDirtyDraft')
    expect(mutationEditorHeader).toContainSource('Edit Timeline')
    expect(mutationEditorHeader).toContainSource('载入所选 Slice')
    expect(mutationEditorHeader).toContainSource('新建模式')
    expect(mutationEditorHeader).toContainSource('放弃草稿并载入')
    expect(mutationEditorHeader).toContainSource('busy: boolean;')
    expect(mutationEditorHeader).toContainSource(':disabled="!hasSelectedSlice || saving || busy"')
    expect(mutationEditorHeader).toContainSource(':disabled="saving || busy"')
    expect(mutationEditorHeader).toContainSource('load-selected-slice')
    expect(mutationEditorHeader).toContainSource('clear-edit-mode')
    expect(mutationEditorHeader).toContainSource('discard-draft-and-load-selected-slice')
    expect(sliceDraftForm).toContainSource('Slice 草稿表单')
    expect(sliceDraftForm).toContainSource('update:time')
    expect(sliceDraftForm).toContainSource('update:mutations')
    expect(sliceDraftForm).toContainSource('validationMessage')
    expect(sliceDraftForm).toContainSource('i-lucide-send')
    expect(sliceDraftForm).toContainSource('<slot />')
    expect(mutationEditor).toContainSource('builderValueMode')
    expect(mutationEditor).toContainSource('builderValueHint')
    expect(mutationEditor).toContainSource('${attr.kind}<${valueType}>')
    expect(mutationEditor).toContainSource('enumValueOptions')
    expect(mutationEditor).toContainSource('refValueOptions')
    expect(mutationEditor).toContainSource('objectBuilderRows')
    expect(mutationEditor).toContainSource('addObjectBuilderRow')
    expect(mutationEditor).toContainSource('syncObjectRowsToBuilderValue')
    expect(mutationEditor).toContainSource('objectHasFixedFields')
    expect(mutationEditor).toContainSource('objectFieldValueMode')
    expect(mutationEditor).toContainSource('const key = rowKey.trim()')
    expect(mutationEditor).toContainSource('!key')
    expect(mutationEditor).toContainSource('attr.itemType')
    expect(mutationEditor).toContainSource('defaultObjectFieldValue(attr)')
    expect(mutationEditor).toContainSource('return "json"')
    expect(mutationEditor).toContainSource('parseJsonObjectBuilderValue')
    expect(mutationEditor).toContainSource('valueType === "object"')
    expect(mutationEditor).toContainSource('mutation value 必须是 JSON object')
    expect(mutationEditor).toContainSource('必须是 JSON object')
    expect(mutationEditor).toContainSource('objectFieldRefOptions')
    expect(subjectCreator).toContainSource('Create Subject')
    expect(subjectCreator).toContainSource('/api/projects/world-engine/subjects')
    expect(subjectCreator).toContainSource('$fetch<CreateSubjectResultDto>')
    expect(subjectCreator).toContainSource('issues: result.issues')
    expect(subjectCreator).toContainSource('const formDisabled = computed(() => props.busy || creating.value);')
    expect(subjectCreator).toContainSource('body: requestBody')
    expect(subjectCreator).toContainSource('type: requestBody.type')
    expect(subjectCreator).toContainSource('form.id = "";')
    expect(subjectCreator).toContainSource('form.name = "";')
    expect(subjectCreator).toContainSource('const lastAppliedDefaultTime = ref("");')
    expect(subjectCreator).toContainSource('function resetFormForProject(): void')
    expect(subjectCreator).toContainSource('form.id = "world";')
    expect(subjectCreator).toContainSource('form.name = "世界";')
    expect(subjectCreator).toContainSource('if (!form.time || form.time === lastAppliedDefaultTime.value) {')
    expect(subjectCreator).toContainSource('watch(() => props.projectRoot, () => {')
    expect(subjectCreator).toContainSource('<fieldset class="space-y-2 disabled:opacity-60" :disabled="formDisabled">')
    expect(sliceInspector).toContainSource('selectedSliceSubjectIds')
    expect(sliceInspector).toContainSource('查询此时状态')
    expect(sliceInspector).toContainSource('查询切面主体')
    expect(sliceInspector).toContainSource('delete-slice')
    expect(sliceInspector).toContainSource('selectedSlice.issues')
    expect(sliceInspector).toContainSource('i-lucide-user-round')
    expect(sliceInspector).toContainSource('i-lucide-trash-2')
    expect(sliceInspector).toContainSource('aria-label="删除 slice"')
    expect(sliceInspector).toContainSource('删除')
    expect(sliceInspector).toContainSource('query-at-slice')
    expect(sliceInspector).toContainSource('query-slice-subjects')
    expect(stateSummary).toContainSource('stateAttrEntries')
    expect(stateSummary).toContainSource('formatStateAttrValue')
    expect(stateSummary).toContainSource('Query 返回')
    expect(stateSummary).toContainSource('attrs</span>')
    expect(timeline).toContainSource('timelineOnlySelectedSubject')
    expect(timeline).toContainSource('timelineSearchText')
    expect(timeline).toContainSource('timelineSliceSearchText')
    expect(timeline).toContainSource('timelineEmptyText')
    expect(timeline).toContainSource('visibleSlices')
    expect(timeline).toContainSource('totalIssueCount')
    expect(timeline).toContainSource('slice.issues')
    expect(timeline).toContainSource('搜索 title / time / summary / mutation')
    expect(timeline).toContainSource('没有匹配当前搜索条件的 slice')
    expect(timeline).toContainSource('当前 subject')
    expect(timeline).toContainSource('edit-slice')
    expect(timeline).toContainSource('aria-label="编辑 slice"')
    expect(timeline).toContainSource('编辑')
    expect(timeline).not.toContainSource('seed-demo')
  })
})
