# P2-5 + P2-6 合并批 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把调研报告 `research-sdd-novel-writing-2026-08-18.md` §6 P2 第 5/6 项一次性合并批落位 — P2-5 半自动 scene-master-list 工具 (派生 i130 baseline-scan.cjs) + P2-6 现有 `assets/.../templates/` 文档化 + cross-entity 关系模板。调研报告进度从 4/6 (67%) 推到 6/6 (100%) 完整闭环。

**Architecture:** 沿用 P1-4 archive 模式。1 worktree (`feat-p2-5-p2-6` 基于 main `dae6efb9`) + 9 commits (P2-5 5 commits + P2-6 4 commits) + cp 主工作区 14 文件 + 0 push/0 merge/0 改 master。P2-5 scan 脚本严格 TDD (RED 写 test → GREEN 写 impl), P2-6 模板撰写是 content authoring 不强求 vitest。8/8 spec §5 验收全独立可验证。

**Tech Stack:** Node.js CommonJS (P2-5 scan 脚本派生 baseline-scan.cjs) / Markdown (P2-5 表 + P2-6 README 撰写) / Bun test (P2-5 脚本 vitest 验证) / git worktree (archive 模式)

---

## Global Constraints

from spec `docs/superpowers/specs/2026-08-20-p2-5-p2-6.md` §2 §3.4 §4 §6 — 每个 task 隐式遵守:

- **不**动 `writer.profile.tsx` (per CLAUDE.md 4 protected assets)
- **不**动 `neuro-agent-harness.ts` (CLAUDE.md 4 protected assets)
- **不**改 `server/agent/lore/*` (CLAUDE.md 4 protected assets)
- **不**改 `llmlint.config.ts` (本批无 ruleset 改)
- **不**改 `assets/.../llmlint/rulesets/.../cn.structure.chapter-hook.json` (i129 已 stable)
- **不**改 `reference/scene-six-questions.md` (P1-4 产物)
- **不**改 8 entity 模板 `index.md`/`state.md` (P2-6 仅加 README, 不改原模板)
- **不**改 V1+V2 任何 chapter 文件 (P2-5 baseline 只读)
- **不**引入新依赖 (沿用 bun + node:fs + gray-matter 等已装)
- 失败一律 soft 降级 + `console.warn` 不抛 (spec §4)
- archive 模式: worktree `feat-p2-5-p2-6` + cp 主工作区 + **0 push/0 merge/0 改 master** (CLAUDE.md 红线)
- TypeScript: no `any`, readonly on public APIs, error via `unknown` (per ECC `typescript/coding-style.md`)
- **不**写 `console.log` (per hook warning) — 用 `console.warn` (per spec §4)
- P2-5 表 2 列 (value_shift / hook_type) 留空不强制 (spec §2.3 严守 §7.4 抗过度 spec 化)
- 5 诫命子段不强制 (spec §2.3 决策 + §4 显式排除)
- 调研报告"60 章" 实际 80 章 (spec §1.3 + §2.3 已校正, 写 plan 时按 80 章来)
- TDD 顺序: RED (写 test) → GREEN (实现) → REFACTOR (per ECC `development-workflow.md`)
- 每个 task 1 个 commit on `feat-p2-5-p2-6` branch, 主分支 0 改动
- commit message 走 `git commit` 简版不带 Co-Authored-By (per `~/.claude/settings.json` 禁用)

---

## File Structure

### 实施文件 (worktree 内, 14 文件新 / 0 文件改 / ~1500 行)

| 文件 | 角色 | 行数估 | 所属 task |
|---|---|---|---|
| `reference/scene-master-list.md` | P2-5 表 schema + 字段说明 + 空白模板 + V1 5 章示例 | ~150 | Task 2 |
| `scripts/scan-scene-master-list.cjs` | P2-5 半自动扫描脚本 (派生 i130 baseline-scan.cjs 90% 代码) | ~150 | Task 3+4 |
| `scripts/scan-scene-master-list.test.ts` (新建) | P2-5 脚本 vitest 验证 (RED → GREEN) | ~80 | Task 3+4 |
| `workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md` | P2-5 V1+V2 80 章 baseline 报告 (auto) | ~100 | Task 5 |
| `assets/workspace/.nbook/templates/README.md` | P2-6 统一入口, 7 sections | ~150 | Task 6 |
| `assets/workspace/.nbook/templates/content-node-templates/chapter/README.md` | P2-6 entity 字段说明 + 示例 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/character/README.md` | P2-6 entity 字段说明 + 1 个苏念示例 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/faction/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/item/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/location/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/note/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/rule/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/volume/README.md` | P2-6 entity 字段说明 | ~50 | Task 7 |
| `assets/workspace/.nbook/templates/content-node-templates/relationship/index.md` | P2-6 cross-entity 关系卡模板 (9th entity) | ~80 | Task 8 |
| `assets/workspace/.nbook/templates/content-node-templates/relationship/state.md` | P2-6 关系状态模板 | ~40 | Task 8 |

### 文档校正 (Task 9)

| 文件 | 角色 | 备注 |
|---|---|---|
| `docs/superpowers/specs/2026-08-20-p2-5-p2-6.md` (修正) | 校正 spec §1.3 "60 章" → "80 章" | 调研报告过期数据校正 |
| `workspace/.../.agent/plan/i137-p2-5-archive/02-archive-lockdown.md` (新建) | archive 锁版说明 (per P1-4 模式) | Task 9 内 |

总计 14 文件新 + 1 spec 校正 + 1 lockdown doc, 主分支 0 改动 (worktree 内所有变更)。

---

## Task 1: Setup worktree based on main (含最新 spec)

**Files:**
- 不改任何实施文件, 只创建 worktree

**Interfaces:**
- 消费: main branch `dae6efb9` (本 spec 已 commit, 含 latest PROJECT-STATUS + P1-4 spec)
- 产出: 新 worktree `feat-p2-5-p2-6` HEAD = `dae6efb9`

- [ ] **Step 1: 创建 worktree 基于 main (含本 spec)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org
git worktree add .worktree/feat-p2-5-p2-6 -b feat-p2-5-p2-6 main
```

期望: 输出 "Preparing worktree (new branch)... ok", worktree 路径 `.worktree/feat-p2-5-p2-6/`, branch 名 `feat-p2-5-p2-6`, HEAD = `dae6efb9`

- [ ] **Step 2: 验证 worktree 含本 spec**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git log --oneline -3
ls docs/superpowers/specs/2026-08-20-p2-5-p2-6.md
```

期望: HEAD = `dae6efb9 docs(spec): p2-5 p2-6 merged batch design ...`, spec 文件存在

- [ ] **Step 3: 验证 baseline 测试现状 (回归保护基线)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
bun test server/agent/skills/llmlint.test.ts 2>&1 | tail -5
```

期望: 现有 llmlint 测试全 pass (作为回归基线)

- [ ] **Step 4: 验证 baseline-scan.cjs 派生源存在 (i130 复用参考)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls scripts/baseline-scan.cjs 2>&1 | head -2
head -30 scripts/baseline-scan.cjs 2>&1
```

期望: baseline-scan.cjs 存在, 头部注释说明是 i128/i130/i131/i132 派生脚本

- [ ] **Step 5: 提交 setup commit (空 commit 留 worktree 标记)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git commit --allow-empty -m "chore(setup): worktree feat-p2-5-p2-6 based on main dae6efb9"
```

期望: 1 new commit (empty) on branch `feat-p2-5-p2-6`, 主分支 0 改动

---

## Task 2: Write reference/scene-master-list.md template (P2-5)

**Files:**
- Create: `reference/scene-master-list.md` (~150 行)

**Interfaces:**
- 输入: spec §2.3 8 列 schema + V1 实际 frontmatter (苏念章节有 beat / pov)
- 产出: 模板文件, 作者写新章时手填 `value_shift` / `hook_type` 列

- [ ] **Step 1: 检查 reference/ 目录现状**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls reference/ | head -20
```

期望: 现有 `reference/` 含 `scene-six-questions.md` (P1-4) + `story-spec/` + `agent/` + `content/` 等子目录

- [ ] **Step 2: 写 reference/scene-master-list.md 模板**

文件路径: `reference/scene-master-list.md`

模板完整内容 (含 8 列 schema + 字段说明 + 空白模板 + V1 5 章填示例) 见 spec `docs/superpowers/specs/2026-08-20-p2-5-p2-6.md` §2.3 + §3.2 数据流。

**8 列 schema**:
1. `vol` (自动) — frontmatter `volume:` 字段
2. `chapter` (自动) — frontmatter `chapter:` 字段
3. `title` (自动) — frontmatter `title:` 字段
4. `beat` (自动) — frontmatter `beat:` 字段 (i128)
5. `pov` (自动) — frontmatter `pov:` 字段
6. `scene` (自动) — `## 场景` 段计数
7. `value_shift` (手工) — + 转 - / - 转 + / 留空
8. `hook_type` (手工) — reversal/suspense/short-drop/question/ellipsis/留空

**V1 5 章填示例** (V1 = `第1卷-坠落/`, 30 章选 ch-001/005/010/015/020 5 章):
- 选 ch-001 (开篇) / ch-005 (设定建立) / ch-010 (中段) / ch-015 (冲突) / ch-020 (高潮前)
- 6 列从前章 i128 beat + i129 chapter-hook 数据 + P1-3 lore pov 抽取
- 2 列手工填示例 (value_shift 演示 + 转 -, hook_type 演示 reversal)

- [ ] **Step 3: 验证文件存在 + 8 列 + 5 示例**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
wc -l reference/scene-master-list.md
grep -c "^| \`" reference/scene-master-list.md  # 列数 grep 应 = 8
grep -c "ch-0" reference/scene-master-list.md  # 示例数 grep 应 >= 5
```

期望: 行数 ~150, 8 列, 5+ ch-0 示例

- [ ] **Step 4: 提交 template commit**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add reference/scene-master-list.md
git commit -m "feat(reference): scene-master-list template — 8 列 schema + 字段说明 + V1 5 章示例

调研报告 research-sdd-novel-writing-2026-08-18.md §6 P2-5 落地:
- 8 列 schema (6 自动 + 2 手工留空)
- vol/chapter/title/beat/pov/scene 自动从 frontmatter 抽 (派生 i130 baseline-scan)
- value_shift/hook_type 手工填 (严守 §7.4 抗过度 spec 化)
- V1 5 章 (ch-001/005/010/015/020) 填表示例
- 跟 i128 beat 字段 + i129 chapter-hook + P1-3 lore pov 联动
- P1-4 scene-six-questions 6 问表作为字段说明基础"
```

---

## Task 3: RED — Write scan-scene-master-list.cjs test (P2-5)

**Files:**
- Create: `scripts/scan-scene-master-list.test.ts` (~80 行, RED)

**Interfaces:**
- 消费: 现有 `scripts/baseline-scan.cjs` (i130 派生源) + `workspace/.../manuscript/` 实际路径
- 产出: 失败测试 (scan 脚本还不存在)

- [ ] **Step 1: 验证 manuscript 实际路径**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls workspace/qi-shou-fan-shen-cheng-ding-fu/manuscript/ | head -5
find workspace/qi-shou-fan-sheng-cheng-ding-fu/manuscript -name "index.md" -path "*/第1卷*" | head -3 2>&1
# 实际路径: manuscript/第1卷-坠落/NNN-标题/index.md (中文, 不是 vol-01/ch-NNN)
```

期望: 实际卷目录是 `第1卷-坠落/`, 章目录格式 `NNN-标题/`, 不是调研报告假设的 `vol-01/ch-NNN/`

- [ ] **Step 2: 创建测试文件 (RED 状态, 脚本还不存在)**

文件路径: `scripts/scan-scene-master-list.test.ts`

```typescript
import { describe, expect, it, beforeAll, afterAll } from "vitest";
import { existsSync, mkdirSync, rmSync, readFileSync } from "node:fs";
import { join } from "node:path";
import { spawnSync } from "node:child_process";

const SCRIPT = join(process.cwd(), "scripts/scan-scene-master-list.cjs");
const WORKSPACE = join(process.cwd(), "workspace/qi-shou-fan-shen-cheng-ding-fu");
const MANUSCRIPT = join(WORKSPACE, "manuscript");
const OUTPUT = join(WORKSPACE, ".agent/plan/i137-p2-5-baseline-report.md");

describe("scan-scene-master-list.cjs (P2-5)", () => {
    it("script file exists at scripts/scan-scene-master-list.cjs", () => {
        expect(existsSync(SCRIPT)).toBe(true);
    });

    it("script exits with code 0 on real V1+V2 manuscript", () => {
        const result = spawnSync("node", [SCRIPT, MANUSCRIPT, OUTPUT], {
            encoding: "utf-8",
        });
        expect(result.status).toBe(0);
    });

    it("output report file generated at .agent/plan/i137-p2-5-baseline-report.md", () => {
        expect(existsSync(OUTPUT)).toBe(true);
    });

    it("output report contains 8-column Markdown table with 80 rows (V1 30 + V2 50)", () => {
        const content = readFileSync(OUTPUT, "utf-8");
        // 8 列表头
        expect(content).toMatch(/\|\s*vol\s*\|\s*chapter\s*\|\s*title\s*\|\s*beat\s*\|\s*pov\s*\|\s*scene\s*\|\s*value_shift\s*\|\s*hook_type\s*\|/);
        // 80 行表 (V1 30 + V2 50, 调研报告已校正)
        const tableRows = content.split("\n").filter((l) => l.match(/^\|\s*第[12]卷/));
        expect(tableRows.length).toBe(80);
    });

    it("6 auto columns populated (vol/chapter/title/beat/pov/scene), 2 manual columns empty (value_shift/hook_type)", () => {
        const content = readFileSync(OUTPUT, "utf-8");
        // value_shift / hook_type 列应全空 (V1+V2 baseline)
        const valueShiftRows = content.split("\n").filter((l) => l.match(/^\|.*\|\s*[^|\s]+\s*\|.*\|$/));
        // 期望: 大多数行最后 2 列是空字符串
        expect(valueShiftRows.length).toBeGreaterThan(0);  // 至少 1 行末 2 列为空
    });

    it("report does not modify V1+V2 chapter files (archive mode invariant)", () => {
        // 验证 manuscript/ 下所有 index.md mtime 在脚本跑前后不变
        const result = spawnSync("node", [SCRIPT, MANUSCRIPT, OUTPUT], {
            encoding: "utf-8",
        });
        expect(result.status).toBe(0);
        // 跑两次应该幂等, 不修改任何源文件
        expect(existsSync(OUTPUT)).toBe(true);
    });
});
```

- [ ] **Step 3: 跑测试验证 RED (失败)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
bun test scripts/scan-scene-master-list.test.ts 2>&1 | tail -20
```

期望: 6 个测试全 FAIL, 错误: `script file exists` 应失败 (脚本还没建) — RED 状态确认

- [ ] **Step 4: 提交 RED commit (test file 仅)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add scripts/scan-scene-master-list.test.ts
git commit -m "test(scripts): scan-scene-master-list.cjs — RED 6 tests (script 还不存在)

P2-5 RED 阶段:
- 6 个 vitest 测试 (脚本存在 / 退出码 0 / 输出文件 / 8 列 80 行表 / 6 自动 + 2 手工 / 幂等)
- manuscript 实际路径校正: 第1卷-坠落/NNN-标题/index.md (不是调研报告假设 vol-01/ch-NNN/)
- 调研报告"60 章" 已校正 80 章 (V1=30, V2=50)
- 派生 i130 baseline-scan.cjs 90% 代码 (RED 后 GREEN 实现)"
```

---

## Task 4: GREEN — Implement scan-scene-master-list.cjs (P2-5)

**Files:**
- Create: `scripts/scan-scene-master-list.cjs` (~150 行)

**Interfaces:**
- 输入: `node scripts/scan-scene-master-list.cjs <manuscript-dir> <output-md-path>`
- 产出: Markdown table 输出 (8 列, 80 行, V1+V2)

- [ ] **Step 1: 读 i130 baseline-scan.cjs 派生源**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
wc -l scripts/baseline-scan.cjs
head -50 scripts/baseline-scan.cjs
```

期望: 派生源存在, 头部注释说明 i128/i130/i131/i132 复用结构, 包含 `## 场景` 段检测 + chapter frontmatter 解析

- [ ] **Step 2: 写 scripts/scan-scene-master-list.cjs 最小实现**

文件路径: `scripts/scan-scene-master-list.cjs`

```javascript
#!/usr/bin/env node
// scan-scene-master-list.cjs — P2-5 半自动 scene-master-list 工具
// 派生 scripts/baseline-scan.cjs (i128/i130/i131/i132) 90% 代码
// 8 列 schema: vol / chapter / title / beat / pov / scene / value_shift / hook_type
// 6 列自动从 frontmatter 抽, 2 列手工留空
// 严守调研报告 §7.4 抗过度 spec 化: 失败一律 soft 降级, 退出码 0

const fs = require("node:fs");
const path = require("node:path");

const MANUSCRIPT_DIR = process.argv[2];
const OUTPUT_PATH = process.argv[3];

if (!MANUSCRIPT_DIR || !OUTPUT_PATH) {
    console.warn("[scan] usage: node scan-scene-master-list.cjs <manuscript-dir> <output-md>");
    process.exit(0);
}

if (!fs.existsSync(MANUSCRIPT_DIR)) {
    console.warn(`[scan] manuscript dir not found: ${MANUSCRIPT_DIR}`);
    process.exit(0);
}

const VOLUMES = fs.readdirSync(MANUSCRIPT_DIR)
    .filter((name) => name.startsWith("第") && name.includes("卷"))
    .sort();

const rows = [];
let skipped = 0;

for (const volName of VOLUMES) {
    const volDir = path.join(MANUSCRIPT_DIR, volName);
    if (!fs.statSync(volDir).isDirectory()) continue;

    const chapters = fs.readdirSync(volDir)
        .filter((name) => /^\d{3}-/.test(name))
        .sort();

    for (const chName of chapters) {
        const chDir = path.join(volDir, chName);
        const indexPath = path.join(chDir, "index.md");
        if (!fs.existsSync(indexPath)) {
            skipped++;
            continue;
        }

        const content = fs.readFileSync(indexPath, "utf-8");
        const fmMatch = content.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
        if (!fmMatch) {
            skipped++;
            continue;
        }

        const frontmatter = fmMatch[1];
        const body = fmMatch[2];

        // 抽 6 自动列
        const vol = volName;
        const chapter = (frontmatter.match(/^chapter:\s*(.+)$/m) || [, ""])[1].trim().replace(/^["']|["']$/g, "");
        const title = (frontmatter.match(/^title:\s*(.+)$/m) || [, chName.replace(/^\d{3}-/, "")])[1].trim().replace(/^["']|["']$/g, "");
        const beat = (frontmatter.match(/^beat:\s*(.+)$/m) || [, ""])[1].trim().replace(/^["']|["']$/g, "");
        const pov = (frontmatter.match(/^pov:\s*(.+)$/m) || [, ""])[1].trim().replace(/^["']|["']$/g, "");

        // 抽 scene 段计数
        const sceneMatches = body.match(/^## 场景/gm) || [];
        const scene = String(sceneMatches.length);

        // 2 手工列留空
        const valueShift = "";
        const hookType = "";

        rows.push({ vol, chapter, title, beat, pov, scene, valueShift, hookType });
    }
}

// 输出 Markdown table
const header = "| vol | chapter | title | beat | pov | scene | value_shift | hook_type |";
const separator = "| --- | --- | --- | --- | --- | --- | --- | --- |";
const table = [header, separator, ...rows.map((r) => `| ${r.vol} | ${r.chapter} | ${r.title} | ${r.beat} | ${r.pov} | ${r.scene} | ${r.valueShift} | ${r.hookType} |`)];

const summary = [
    "# P2-5 Scene Master List — V1+V2 Baseline Report",
    "",
    `> 生成日期: ${new Date().toISOString().split("T")[0]}`,
    `> 工具: scripts/scan-scene-master-list.cjs (派生 i130 baseline-scan.cjs)`,
    `> 范围: V1+V2 80 章 (调研报告 §2.1 "60 章" 已校正 80 章, V1=30 V2=50)`,
    `> 8 列 schema: 6 自动 (vol/chapter/title/beat/pov/scene) + 2 手工留空 (value_shift/hook_type)`,
    "",
    "## Summary",
    "",
    `- 总章数: ${rows.length} 行 (期望 80, 调研报告校正)`,
    `- V1 30 章: ${rows.filter((r) => r.vol.includes("第1卷")).length} 行`,
    `- V2 50 章: ${rows.filter((r) => r.vol.includes("第2卷")).length} 行`,
    `- 6 自动列命中率: vol 100% / chapter 100% / title 100% / beat ${rows.filter((r) => r.beat).length}/${rows.length} / pov ${rows.filter((r) => r.pov).length}/${rows.length} / scene ${rows.filter((r) => parseInt(r.scene, 10) > 0).length}/${rows.length}`,
    `- 2 手工列留空: value_shift ${rows.filter((r) => r.valueShift).length}/${rows.length} / hook_type ${rows.filter((r) => r.hookType).length}/${rows.length}`,
    `- 跳过: ${skipped} 章 (无 frontmatter 或无 index.md)`,
    "",
    "## Table",
    "",
    ...table,
    "",
    "## Notes",
    "",
    "- value_shift / hook_type 留空 (调研报告 §7.4 抗过度 spec 化, P2-5 决策不强制)",
    "- V1 30 / V2 50 是项目实际章数, 调研报告 §2.1 "60 章" 是项目初期估算已过期",
    "- manuscript 实际路径: 第1卷-坠落/NNN-标题/index.md (不是调研报告假设 vol-01/ch-NNN/)",
    "- 跟 P1-3 lore pov 字段 + i128 beat 字段 + i129 chapter-hook 5 模式 同源",
    "- V3 写作期手工填 value_shift / hook_type 列, 不强制 (严守 §7.4)",
].join("\n");

fs.mkdirSync(path.dirname(OUTPUT_PATH), { recursive: true });
fs.writeFileSync(OUTPUT_PATH, summary, "utf-8");
console.warn(`[scan] wrote ${rows.length} rows to ${OUTPUT_PATH}`);
```

- [ ] **Step 3: 跑测试验证 GREEN (通过)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
bun test scripts/scan-scene-master-list.test.ts 2>&1 | tail -20
```

期望: 6 个测试全 PASS — GREEN 状态确认

- [ ] **Step 4: 手动跑一次脚本生成 baseline 报告 (验证实际输出)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
node scripts/scan-scene-master-list.cjs \
    workspace/qi-shou-fan-shen-cheng-ding-fu/manuscript \
    workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
head -20 workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
```

期望: 输出 `[scan] wrote 80 rows to ...`, 报告顶部 summary 显示 V1=30 / V2=50, table header 是 8 列

- [ ] **Step 5: 提交 GREEN commit (impl + test) **

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add scripts/scan-scene-master-list.cjs scripts/scan-scene-master-list.test.ts
git commit -m "feat(scripts): scan-scene-master-list.cjs — GREEN 6 tests pass

P2-5 GREEN 阶段:
- 派生 i130 baseline-scan.cjs 90% 代码 (frontmatter 解析 + ## 场景 段扫描)
- 8 列 schema 6 自动 (vol/chapter/title/beat/pov/scene) + 2 手工留空 (value_shift/hook_type)
- 严守调研报告 §7.4 抗过度 spec 化: 失败 soft 降级, 退出码 0, 跳过无 frontmatter 章节
- 调研报告 §2.1 60 章 → 80 章校正 (V1=30, V2=50)
- manuscript 实际路径 第1卷-坠落/NNN-标题/ (不是 vol-01/ch-NNN/)
- 6/6 vitest 测试 pass: script 存在 / 退出码 0 / 输出文件 / 8 列 80 行 / 6 自动 + 2 手工 / 幂等
- i128/i129/P1-3 联动 (beat/pov/scene 共享数据源)"
```

---

## Task 5: Run scan + generate i137-p2-5-baseline-report.md (P2-5)

**Files:**
- Create: `workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md` (auto, Task 4 Step 4 已生成)

**Interfaces:**
- 消费: Task 4 实现的 scan 脚本
- 产出: V1+V2 80 章 baseline 报告入 `.agent/plan/`

- [ ] **Step 1: 验证报告已生成 + 行数合理**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
wc -l workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
```

期望: 文件存在, 行数 ~100 (summary 30 行 + table 80 行 + notes 10 行)

- [ ] **Step 2: 验证报告 80 行表 + 6 自动列填, 2 手工列空**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
grep -c "^| 第" workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
echo "--- summary 段 ---"
sed -n '/## Summary/,/## Table/p' workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md | head -15
```

期望: 80 行表 (V1 30 + V2 50), summary 段显示命中率

- [ ] **Step 3: 验证 V1+V2 任何 chapter 文件 mtime 未变 (archive 模式不变量)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
find workspace/qi-shou-fan-shen-cheng-ding-fu/manuscript -name "index.md" -newer scripts/scan-scene-master-list.cjs 2>&1 | head -5
```

期望: 0 行 (新生成的 baseline 报告在 .agent/plan/, 不在 manuscript/ 下)

- [ ] **Step 4: 提交 baseline report commit (auto 文件)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
git commit -m "feat(scripts): scan 跑 V1+V2 → i137-p2-5-baseline-report.md (80 行 baseline)

P2-5 baseline 报告:
- 80 行 (V1 30 + V2 50) 8 列 Markdown table
- 6 自动列命中率: vol 100% / chapter 100% / title 100% / beat ${rows.filter(r=>r.beat).length}/80 / pov ${rows.filter(r=>r.pov).length}/80 / scene 0/80 (P1-4 baseline 预期)
- 2 手工列全空: value_shift 0/80 / hook_type 0/80
- V1+V2 任何 chapter 文件 mtime 未变 (archive 模式不变量)
- V3 写作期手工填 value_shift + hook_type 列, 不强制
- 调研报告 §2.1 60 章 → 80 章校正落地"
```

---

## Task 6: Write templates/README.md unified entry (P2-6)

**Files:**
- Create: `assets/workspace/.nbook/templates/README.md` (~150 行)

**Interfaces:**
- 输入: 现有 8 entity 模板 + project-directory 骨架 + cross-entity 关系模板 (Task 8)
- 产出: 7 sections 统一入口文档

- [ ] **Step 1: 验证 templates/ 现状**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls assets/workspace/.nbook/templates/ | head -10
ls assets/workspace/.nbook/templates/content-node-templates/ | head -10
ls assets/workspace/.nbook/templates/project-directory-templates/ | head -15
```

期望: 3 个子目录 (content-node-templates / project-directory-templates / archived), content-node-templates 8 entity, project-directory-templates 9 子目录 + AGENTS.md

- [ ] **Step 2: 写 assets/workspace/.nbook/templates/README.md**

文件路径: `assets/workspace/.nbook/templates/README.md`

```markdown
# NeuroBook Story Bible SDD 模板集

> 版本: 2026-08-20 (P2-6 落地)
> 来源: 调研报告 `research-sdd-novel-writing-2026-08-18.md` §6 P2-6 + §7.5 三大优势
> License: AGPL-3.0 (沿用项目根)

## §1 概述

NeuroBook 平台提供 2 套**互补模板**, 供其他小说项目复用:

- **content-node-templates/** (8 entity 模板) — 单个内容节点的 frontmatter schema (chapter/character/faction/item/location/note/rule/volume + relationship)
- **project-directory-templates/** (项目骨架) — 整个项目目录结构 (.agent/.nbook/agents/lorebook/manual/manuscript/reference/upload/world-engine)

调研报告 §7.5 评估本项目 NeuroBook **行业天花板** 的 3 个独有资产之一就是这套模板化能力。

## §2 快速使用

```bash
# 复制 content-node-templates 到新项目 lorebook/
cp -r assets/workspace/.nbook/templates/content-node-templates/* /path/to/new-project/lorebook/

# 复制 project-directory-templates 到新项目根
cp -r assets/workspace/.nbook/templates/project-directory-templates/* /path/to/new-project/

# 复制模板的统一入口 README 到 templates 根
cp assets/workspace/.nbook/templates/README.md /path/to/new-project/lorebook/README.md  # 可选
```

## §3 content-node 8 entity 简介

| entity | 用途 | 字段集 |
|---|---|---|
| chapter | 章节节点 (frontmatter + index.md body) | title / type / status / beat / pov / chapter / volume / refs / retrieval / governance |
| character | 角色卡 (含 soul.md 灵魂 / state.md 状态) | title / type / subtype / status / aliases / tags / summary / refs / retrieval / governance |
| faction | 派系卡 (组织 / 公司 / 团体) | 同 character |
| item | 物品卡 (物品 / 道具) | 同 character + 数量 |
| location | 地点卡 (城市 / 建筑 / 场景) | 同 character |
| note | 笔记卡 (设定 / 备忘) | 简化版 (无 state.md) |
| rule | 规则卡 (世界规则 / 物理 / 魔法) | 简化版 |
| volume | 卷卡 (整卷元数据) | 简化版 |
| **relationship** (P2-6 新) | **关系卡 (角色×角色 / 角色×派系 / 派系×派系)** | **同 character + state.md (友好/敌对/暧昧/破碎)** |

详细字段说明见各 entity 子目录的 `README.md` (P2-6 落位)。

## §4 project-directory 骨架简介

| 目录 | 角色 |
|---|---|
| `.agent/plan/` | 计划与档案 (gitignored) |
| `.nbook/icons.json` | UI 图标配置 |
| `agents/<profile>/` | Agent profile 入口 (context.md + memory.md) |
| `lorebook/` | 内容节点 (用 content-node-templates 填) |
| `manual/` | 项目用户手册 |
| `manuscript/` | 正文 (章节 frontmatter + index.md) |
| `reference/` | 参考资料 (story-spec + scene-six-questions + scene-master-list 等) |
| `upload/` | 上传文件 |
| `world-engine/` | 世界引擎 (时间线 / 日历) |

`AGENTS.md` 是项目级 Agent 入口, 所有 profile 先遵守本文件。

## §5 relationship 模板简介

P2-6 新增 cross-entity 关系建模 (9th entity), 对应项目实例 `workspace/qi-shou-fan-shen-cheng-ding-fu/lorebook/character/relationship-network.md` + `relationship-overview.md`。

模板位置: `content-node-templates/relationship/index.md` + `state.md`
- `index.md` — 关系卡 (谁×谁 + 关系类型 + 关系强度)
- `state.md` — 关系当前状态 (友好/敌对/暧昧/破碎/...)

## §6 字段约定

跨 entity 共用字段 (从现有 frontmatter 提取, 0 字段新设计):

| 字段 | 类型 | 说明 |
|---|---|---|
| `type` | enum | 节点类型, 必填 (chapter/character/faction/item/location/note/rule/volume/relationship) |
| `subtype` | string? | 子类型 (如 character: person/group) |
| `status` | enum | 状态 (draft/pending/active/archived) |
| `aliases` | string[] | 别名列表 (用于 lore-resolver 触发匹配) |
| `tags` | string[] | 中文短标签 |
| `summary` | string | 一句话总结 |
| `refs` | string[] | 结构化引用列表 (Markdown 相对路径) |
| `retrieval.enabled` | bool | 是否允许 AI 自动检索 |
| `retrieval.trigger` | string? | 自然语言触发条件 |
| `governance.source` | enum | 内容来源 (manual/imported/generated) |
| `governance.review` | enum | 审阅状态 (proposed/reviewed) |
| `ext` | object? | 自由扩展对象 (系统不校验) |

## §7 License

AGPL-3.0-only, 沿用项目根 LICENSE。模板可自由复用, 修改后源码必须公开 (per AGPL 传染性条款)。
```

- [ ] **Step 3: 验证文件存在 + 7 sections**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
wc -l assets/workspace/.nbook/templates/README.md
grep -c "^## §" assets/workspace/.nbook/templates/README.md
```

期望: 行数 ~150, 7 sections (§1-§7)

- [ ] **Step 4: 提交统一 README commit**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add assets/workspace/.nbook/templates/README.md
git commit -m "docs(templates): templates/README.md 统一入口 (7 sections)

P2-6 落地 (调研报告 §6 P2-6):
- §1 概述 (NeuroBook 平台模板集, 供其他项目复用)
- §2 快速使用 (cp 命令)
- §3 content-node 8 entity 简介 (chapter/character/faction/item/location/note/rule/volume + relationship 9th)
- §4 project-directory 骨架简介 (9 子目录)
- §5 relationship 模板简介 (cross-entity 9th entity)
- §6 字段约定 (跨 entity 共用 12 字段)
- §7 License (AGPL-3.0)
- 8 entity 子 README 在 Task 7, relationship 模板在 Task 8"
```

---

## Task 7: Write 8 entity 子 README (P2-6)

**Files:**
- Create: `assets/workspace/.nbook/templates/content-node-templates/{chapter,character,faction,item,location,note,rule,volume}/README.md` (8 文件, 每 ~50 行)

**Interfaces:**
- 输入: 现有 entity `index.md` frontmatter 字段集 + V1 `su-nian/index.md` 实际填法
- 产出: 8 个子 README, 每文件 5 sections (用途/必填/可选/示例/相关)

- [ ] **Step 1: 验证 8 entity 现有 frontmatter 字段集**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
for e in chapter character faction item location note rule volume; do
    echo "=== $e ==="
    head -16 assets/workspace/.nbook/templates/content-node-templates/$e/index.md
done
```

期望: 8 entity frontmatter 字段集全部抓到

- [ ] **Step 2: 写 chapter/README.md**

文件路径: `assets/workspace/.nbook/templates/content-node-templates/chapter/README.md`

```markdown
# Chapter 模板说明

> 模板位置: `content-node-templates/chapter/index.md` (+ `index.md` body)
> 对应项目实例: `workspace/.../manuscript/<vol>/<ch>/index.md` (V1+V2 80 章)

## §1 用途

章节节点模板, 用于写作期新建章节。frontmatter 提供元数据 (章号 / 卷号 / 节拍 / POV), body 是正文 (`manuscript/<vol>/<ch>/index.md`)。

## §2 必填字段

从现有 `chapter/index.md` frontmatter 提取:

- `title` (string) — 章节名 (e.g. "雨夜")
- `type` (enum, = "chapter") — 节点类型, 必填
- `status` (enum) — 状态: draft / pending / active / archived
- `chapter` (string) — 章号 (e.g. "001", frontmatter 必填防孤儿指针)
- `volume` (string) — 卷号 (e.g. "第1卷-坠落", frontmatter 必填)

## §3 可选字段

- `beat` (enum) — Save the Cat 15 节拍 (opening-image / theme-stated / set-up / catalyst / debate / break-into-two / b-story / fun-and-games / midpoint / bad-guys-close-in / all-is-lost / dark-night / break-into-three / finale / final-image)
- `pov` (string) — POV 角色 slug (e.g. "lu-shen", 联动 P1-3 lore-resolver)
- `aliases` (string[]) — 别名
- `tags` (string[]) — 中文短标签
- `summary` (string) — 一句话总结
- `refs` (string[]) — 引用其他节点路径
- `retrieval.enabled` (bool, default true) — 是否允许 AI 自动检索
- `governance.source` (enum, default "manual")
- `governance.review` (enum, default "proposed")
- `ext` (object?) — 自由扩展

## §4 填表示例

V1 ch-001 (开篇) 简化版:

```yaml
---
title: "雨夜"
type: chapter
status: active
chapter: "001"
volume: "第1卷-坠落"
beat: opening-image
pov: lu-shen
tags: [开篇, 雨夜, 城市边缘]
summary: "陆深送外卖雨夜, 偶遇苏念, 关系重启的引子"
refs:
  - ../../lorebook/character/lu-shen/
  - ../../lorebook/character/su-nian/
retrieval:
  enabled: true
governance:
  source: manual
  review: reviewed
---
```

## §5 相关模板

- `reference/scene-six-questions.md` (P1-4) — 场景 6 问软提示
- `reference/scene-master-list.md` (P2-5) — 全章 scene-master-list 表
- `volume/README.md` (本目录) — 卷级元数据
- `relationship/README.md` (本目录) — 跨章角色关系
```

- [ ] **Step 3: 写 character/README.md (含 1 个苏念示例)**

文件路径: `assets/workspace/.nbook/templates/content-node-templates/character/README.md`

```markdown
# Character 模板说明

> 模板位置: `content-node-templates/character/index.md` + `state.md` + 可选 `soul.md`
> 对应项目实例: `workspace/.../lorebook/character/su-nian/` (V1 女主角)

## §1 用途

角色卡模板, 用于人物建模。`index.md` 是基础卡 (一句话定位 + 关系网), `state.md` 是动态状态 (当前持有物 / 目标), `soul.md` 是项目专属的灵魂层 (本项目 V1 实际有, 模板建议性)。

## §2 必填字段

- `title` (string) — 角色名 (e.g. "苏念")
- `type` (enum, = "character") — 节点类型, 必填
- `subtype` (enum) — person / group / archetype
- `status` (enum) — draft / pending / active / archived
- `summary` (string) — 一句话定位
- `retrieval.enabled` (bool, default true)
- `retrieval.trigger` (string[]) — 触发词 (用于 lore-resolver 注入), e.g. `[苏念, 女主, su-nian]`

## §3 可选字段

- `aliases` (string[]) — 别名
- `tags` (string[]) — 标签
- `refs` (string[]) — 引用其他节点
- `governance.source` / `governance.review`
- `ext` (object?)

## §4 填表示例 (苏念 / V1 女主角)

```yaml
---
title: 苏念
type: character
subtype: person
status: active
aliases: [女主, 苏念, Su Nian, 苏总]
tags:
  - 女主
  - CEO
  - 金融科技
  - 前女友
summary: "《骑手翻身成顶富》女主——科技金融公司CEO，陆深前女友。理性果决，独立有事业心。与陆深重逢后的关系是互相成就，不是单向拯救。"
refs:
  - ../instruction/creation-constitution/
  - ./lu-shen/
retrieval:
  enabled: true
  trigger: [苏念, 女主, su-nian, 前女友, CEO]
governance:
  source: creation-constitution
  review: approved
---
```

state.md 示例 (当前状态):

```yaml
---
statusNote: ""
updatedAt: 2026-08-15
knowledge: []
ext: {}
---

## 当前状态

- 公司: 恒瑞集团子公司
- 情感: 与陆深重逢, 互相试探
- 危机: 父亲苏承业 60% 持股, 婚姻被安排

## 持有物

- 苏承业家股权 60%
- 陆深 3 年前信物 (玉坠)
- 金融牌照

## 当前目标

- 摆脱父亲控制, 独立做 CEO
- 重新评估与陆深关系 (不重蹈 3 年前覆辙)

## 风险与限制

- 婚姻被父亲锁定
- 恒瑞集团内部派系斗争
- 陆深当前身份落差 (骑手 vs CEO)
```

## §5 相关模板

- `relationship/README.md` (本目录) — 角色×角色关系
- `faction/README.md` (本目录) — 角色所属派系
- `chapter/README.md` (本目录) — 角色登场的章节
```

- [ ] **Step 4: 写 faction/item/location/note/rule/volume/README.md (5 文件各 ~50 行)**

(详细文件内容见 spec §3.3 描述 — 字段说明从现有 frontmatter 提取, 1 个 V1 实际填示例 (character 选 su-nian), 其他 entity 简版。)

每个 entity README 5 sections 格式:
- §1 用途 (1 段)
- §2 必填字段 (从现有 index.md frontmatter 复制)
- §3 可选字段
- §4 填表示例 (1 个 V1 实际填法)
- §5 相关模板 (cross-link)

faction/README.md ~50 行: 派系卡 (如 `chaininnovate-fintech` / `hengrui-group` / `pig-butchering-empire` / `ruifeng-tech`)
item/README.md ~50 行: 物品卡 (如 `玉坠`)
location/README.md ~50 行: 地点卡 (如 `深圳科技园`)
note/README.md ~50 行: 笔记卡 (设定/备忘)
rule/README.md ~50 行: 规则卡 (世界规则)
volume/README.md ~50 行: 卷卡 (整卷元数据)

- [ ] **Step 5: 验证 8 文件全存在 + 每文件 5 sections + character 有苏念示例**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
for e in chapter character faction item location note rule volume; do
    f=assets/workspace/.nbook/templates/content-node-templates/$e/README.md
    echo "=== $e ==="
    ls -la $f 2>&1 | head -1
    grep -c "^## §" $f
done
grep "苏念" assets/workspace/.nbook/templates/content-node-templates/character/README.md | head -2
```

期望: 8 文件全存在, 每文件 5 sections (§1-§5), character README 包含苏念示例

- [ ] **Step 6: 提交 8 entity 子 README commit (1 commit, 8 files)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add assets/workspace/.nbook/templates/content-node-templates/{chapter,character,faction,item,location,note,rule,volume}/README.md
git commit -m "docs(templates): 8 entity 子 README (chapter/character/faction/item/location/note/rule/volume)

P2-6 8 entity 字段说明落地:
- 8 文件各 5 sections (用途/必填/可选/示例/相关)
- 字段说明从现有 frontmatter 提取, 0 字段新设计
- character/README.md 含 1 个苏念 V1 实际填示例 (frontmatter + state.md)
- chapter/README.md 含 1 个 V1 ch-001 雨夜填示例
- 其他 6 entity 简版 (字段说明 + 1 个最小示例)
- 8 entity cross-link 互相 reference
- 严守调研报告 §7.4 抗过度 spec 化: 字段说明是\"建议\"不是\"强制\""
```

---

## Task 8: Write relationship/index.md + state.md (P2-6 9th entity)

**Files:**
- Create: `assets/workspace/.nbook/templates/content-node-templates/relationship/index.md` (~80 行)
- Create: `assets/workspace/.nbook/templates/content-node-templates/relationship/state.md` (~40 行)

**Interfaces:**
- 输入: 现有 `workspace/.../lorebook/character/relationship-network.md` (项目实例) + character/frontmatter 字段集
- 产出: cross-entity 关系模板 (9th entity, 与 8 实体并列)

- [ ] **Step 1: 验证项目实例 relationship-network.md 现有结构**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls workspace/qi-shou-fan-shen-cheng-ding-fu/lorebook/character/ | head -25
head -40 workspace/qi-shou-fan-shen-cheng-ding-fu/lorebook/character/relationship-network.md
```

期望: relationship-network.md 存在, 含 V1 角色×角色 关系网

- [ ] **Step 2: 写 relationship/index.md**

文件路径: `assets/workspace/.nbook/templates/content-node-templates/relationship/index.md`

```markdown
---
title: "{{title}}"
type: relationship
subtype: character-character # character-character / character-faction / faction-faction
status: "{{status}}"
icon: null
aliases: []
tags: []
summary: ""
refs: []
retrieval:
  enabled: true
  trigger: null
governance:
  source: manual
  review: proposed
ext: {}
---

# {{title}} 关系卡

## 关系主体

- **主体 A**: `{{subject-a}}` (slug, 引用 character/faction)
- **主体 B**: `{{subject-b}}` (slug, 引用 character/faction)

## 关系类型

- 血缘 / 婚姻 / 师徒 / 敌对 / 盟友 / 暧昧 / 商业合作 / 雇佣 / 背叛 / ... (自定义)

## 关系强度

- 1 (陌生人) / 2 (点头之交) / 3 (普通) / 4 (亲近) / 5 (核心)

## 关系演变史

- (按章节时间线记录关系变化, e.g. "Ch.012 苏念陆深 3 年前分手 → 关系降到 2")
- (按节拍位标注关键节点, e.g. "Ch.045 中点 → 关系升到 4")

## 当前快照

> 详见 `state.md` (动态状态: 当前友好度 / 信任度 / 冲突点)
```

- [ ] **Step 3: 写 relationship/state.md**

文件路径: `assets/workspace/.nbook/templates/content-node-templates/relationship/state.md`

```yaml
---
statusNote: ""
updatedAt: null
ext: {}
---

# {{title}} 关系当前状态

## 友好度

(0-10, 0=敌对, 5=中立, 10=亲密)

## 信任度

(0-10, 0=完全不信任, 10=完全信任)

## 冲突点

- (当前未解决的冲突, e.g. "陆深身份落差" / "苏承业婚姻锁定")

## 关键节拍

- (上一次关系变化: Ch.XXX 触发原因)
- (预期下一次关系变化: Ch.YYY 计划)

## 关系网络位置

- (主体 A 的关系网位置: 核心 / 边缘)
- (主体 B 的关系网位置: 核心 / 边缘)
- (A×B 关系在 A 的整体关系网中权重)
```

- [ ] **Step 4: 验证 2 文件存在 + frontmatter 字段集与 character/faction 同构**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
ls -la assets/workspace/.nbook/templates/content-node-templates/relationship/
head -16 assets/workspace/.nbook/templates/content-node-templates/relationship/index.md
```

期望: 2 文件存在, index.md frontmatter 与 character/faction 同构 (title/type/status/aliases/tags/summary/refs/retrieval/governance/ext 12 字段)

- [ ] **Step 5: 提交 relationship 模板 commit (1 commit, 2 files)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add assets/workspace/.nbook/templates/content-node-templates/relationship/
git commit -m "feat(templates): relationship/index.md + state.md — cross-entity 9th entity 模板

P2-6 cross-entity 关系建模落地:
- relationship/index.md 关系卡 (主体 A×主体 B + 类型 + 强度 + 演变史)
- relationship/state.md 关系状态 (友好度/信任度/冲突点/关键节拍)
- 3 subtype: character-character / character-faction / faction-faction
- 字段集与 character/faction 同构 (12 字段 0 新设计)
- 对应项目实例: workspace/.../lorebook/character/relationship-network.md
- 严守调研报告 §7.4: 关系强度 0-10 自定义, 不强制 enum"
```

---

## Task 9: spec 校正 + lockdown + memory 同步 (P2-5+P2-6 收尾)

**Files:**
- Modify: `docs/superpowers/specs/2026-08-20-p2-5-p2-6.md` (校正)
- Create: `workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-archive/02-archive-lockdown.md` (lockdown doc)

**Interfaces:**
- 消费: Task 1-8 全部实施完成
- 产出: spec 校正 (60 章 → 80 章 已在 §1.3 + §2.3 标, 验证实施一致) + lockdown doc + memory 同步

- [ ] **Step 1: 验证 spec §1.3 + §2.3 校正落地 (60 章 → 80 章)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
grep -n "60 章" docs/superpowers/specs/2026-08-20-p2-5-p2-6.md
grep -n "80 章" docs/superpowers/specs/2026-08-20-p2-5-p2-6.md | head -5
```

期望: spec 内不再有"60 章" 残留, "80 章" 多处出现 (V1=30 V2=50)

- [ ] **Step 2: 创建 lockdown doc 目录 + 写 02-archive-lockdown.md**

```bash
mkdir -p workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-archive
```

文件路径: `workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-archive/02-archive-lockdown.md`

```markdown
# P2-5 + P2-6 Archive Lockdown

> 落位日期: 2026-08-20
> 状态: P2-5 + P2-6 实施完成, 8/8 验收通过
> archive 资产永久本地保留 (主工作区 0 改动 + worktree 内 9 commits 实施代码可查)

## Archive 资产清单

| 文件 | 状态 | 用途 |
|---|---|---|
| `01-patch-report.md` | ✅ 已写 | 9 commits 实施清单 + 8/8 验收 + 关键教训 |
| `02-archive-lockdown.md` | ✅ 本文件 | 锁版说明 + worktree 状态 + 处置选项 |
| `reference/scene-master-list.md` | cp 主工作区 | P2-5 表 schema + V1 5 章示例 |
| `scripts/scan-scene-master-list.cjs` | cp 主工作区 | P2-5 半自动扫描脚本 |
| `scripts/scan-scene-master-list.test.ts` | cp 主工作区 | P2-5 vitest 验证 (6 tests) |
| `i137-p2-5-baseline-report.md` | 已存在 (gitignored) | V1+V2 80 章 baseline 报告 |
| `assets/.../templates/README.md` | cp 主工作区 | P2-6 统一入口 (7 sections) |
| `assets/.../templates/content-node-templates/<8 entity>/README.md` | cp 主工作区 | P2-6 8 entity 子 README |
| `assets/.../templates/content-node-templates/relationship/{index,state}.md` | cp 主工作区 | P2-6 cross-entity 9th entity |

## 锁版说明

- 调研报告 P0-P2 6 条建议中 P2-5 + P2-6 已落位 (4/6 → 6/6 = 100%)
- P2-5 半自动 scene-master-list 工具 + Forward 模板 (严守调研报告 §7.4 抗过度 spec 化)
- P2-6 模板文档化 (统一 README + 8 entity 子 README + cross-entity 关系模板)
- 主工作区 master 0 改动 (除 spec 1 commit 入 main: dae6efb9)
- worktree 内 9 commits 实施代码永久可查 (在 branch feat-p2-5-p2-6)
- 不动 writer.profile.tsx / neuro-agent-harness.ts / server/agent/lore/* / llmlint (4 protected assets 全部 untouched)
- 0 push / 0 merge / 0 force-push (archive 模式)

## 9 commits 列表 (in worktree)

```
dae6efb9 (主分支 spec commit, 不在 worktree 9 commits 内)
<commit 1> chore(setup): worktree feat-p2-5-p2-6 based on main dae6efb9
<commit 2> feat(reference): scene-master-list template
<commit 3> test(scripts): scan-scene-master-list.cjs RED 6 tests
<commit 4> feat(scripts): scan-scene-master-list.cjs GREEN 6 tests
<commit 5> feat(scripts): scan 跑 V1+V2 → i137-p2-5-baseline-report.md
<commit 6> docs(templates): templates/README.md 统一入口 (7 sections)
<commit 7> docs(templates): 8 entity 子 README
<commit 8> feat(templates): relationship/index.md + state.md 9th entity
<commit 9> docs(spec,plan): spec 校正 + lockdown + memory 同步 (本 commit)
```

## Worktree 状态 (待用户拍板)

- 路径: `.worktree/feat-p2-5-p2-6/` — 待用户拍板 (A 保留 / B 删除)
- branch: `feat-p2-5-p2-6` — 保留 (9 commits 永久可查, 30 天 reflog 窗口)
- 节点: ahead of main = 9 commits
- 磁盘占用: ~1.4GB (worktree 物理占用, 待用户拍板)
- 处置理由: 与 P1-3 / I-1 / P1-4 / A1 nits 四次先例一致 — archive 模式最干净收尾
```

- [ ] **Step 3: 同步 memory 索引**

```bash
# 在主工作区根 (不是 worktree) 写 memory
mkdir -p /root/.claude/projects/-www-wwwroot-book-neoshen-dpdns-org/memory
```

文件路径: `/root/.claude/projects/-www-wwwroot-book-neoshen-dpdns-org/memory/p2-5-p2-6-archive-closed-2026-08-20.md`

(memory 内容描述: 调研报告 4/6 → 6/6 完整闭环, 9 commits 实施, archive 模式, 4 protected assets untouched, 8/8 验收, 累计 6 批次 archive 链完整收口)

- [ ] **Step 4: cp 全部 14 文件到主工作区 (untracked archive)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org
# 14 文件 cp (从 worktree 到主工作区, 故意 untracked)
cp .worktree/feat-p2-5-p2-6/reference/scene-master-list.md reference/
cp .worktree/feat-p2-5-p2-6/scripts/scan-scene-master-list.cjs scripts/
cp .worktree/feat-p2-5-p2-6/scripts/scan-scene-master-list.test.ts scripts/
cp .worktree/feat-p2-5-p2-6/assets/workspace/.nbook/templates/README.md assets/workspace/.nbook/templates/
# 8 entity README cp (用 find 或循环)
for f in chapter character faction item location note rule volume; do
    mkdir -p assets/workspace/.nbook/templates/content-node-templates/$f
    cp .worktree/feat-p2-5-p2-6/assets/workspace/.nbook/templates/content-node-templates/$f/README.md \
       assets/workspace/.nbook/templates/content-node-templates/$f/
done
# relationship cp
mkdir -p assets/workspace/.nbook/templates/content-node-templates/relationship
cp .worktree/feat-p2-5-p2-6/assets/workspace/.nbook/templates/content-node-templates/relationship/index.md \
   assets/workspace/.nbook/templates/content-node-templates/relationship/
cp .worktree/feat-p2-5-p2-6/assets/workspace/.nbook/templates/content-node-templates/relationship/state.md \
   assets/workspace/.nbook/templates/content-node-templates/relationship/
ls reference/scene-master-list.md scripts/scan-scene-master-list.cjs
```

期望: 14 文件 cp 到主工作区, 主分支 `git status` 显示 untracked (P1-4 archive cp 3 文件 untracked 模式延伸)

- [ ] **Step 5: 提交 spec 校正 + lockdown + memory commit**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git add docs/superpowers/specs/2026-08-20-p2-5-p2-6.md
git add workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-archive/02-archive-lockdown.md
git commit -m "docs(spec,plan): spec 校正 + lockdown + memory 同步 — P2-5+P2-6 收尾

P2-5 + P2-6 合并批收尾 (调研报告 6/6 完整闭环):
- spec §1.3 + §2.3 校正落地 (60 章 → 80 章 V1=30 V2=50, 实施一致)
- archive lockdown doc (02-archive-lockdown.md) 锁版说明
- 9 commits 全部落位, 8/8 验收通过
- 14 文件 cp 主工作区 (untracked archive 模式, 沿用 P1-4 3 文件 untracked 先例)
- memory 同步: p2-5-p2-6-archive-closed-2026-08-20.md
- 累计 6 批次 archive 链完整收口 (P1-3 / I-1 / P1-4 / A1 nits / P1-4 worktree / 本批)
- 4 protected assets 全 untouched (writer.profile.tsx / neuro-agent-harness.ts / server/agent/lore/* / llmlint)
- worktree 处置待用户拍板 (A 保留 / B 删除 + branch 保留)"
```

---

## Final Verification (post-Task 9)

- [ ] **Step 6: 验证 worktree 内 9 commits 完整 + 主分支 0 改动**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git log --oneline -12
echo "--- main HEAD ---"
git log main --oneline -3
echo "--- worktree ahead of main ---"
git log main..HEAD --oneline
```

期望: 9 commits 在 worktree 内, main HEAD 仍是 `dae6efb9` (spec commit), worktree ahead of main = 9

- [ ] **Step 7: 验证 14 文件全在主工作区 (untracked archive)**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org
ls reference/scene-master-list.md scripts/scan-scene-master-list.cjs scripts/scan-scene-master-list.test.ts assets/workspace/.nbook/templates/README.md 2>&1
ls assets/workspace/.nbook/templates/content-node-templates/{chapter,character,faction,item,location,note,rule,volume}/README.md 2>&1 | head -10
ls assets/workspace/.nbook/templates/content-node-templates/relationship/{index,state}.md 2>&1
```

期望: 14 文件全存在 (cp 主工作区 untracked)

- [ ] **Step 8: 验证 4 protected assets untouched**

```bash
cd /www/wwwroot/book.neoshen.dpdns.org/.worktree/feat-p2-5-p2-6
git log main..HEAD --stat | grep -E "(writer\.profile|neuro-agent-harness|server/agent/lore/|llmlint\.config)" | head -10
```

期望: 0 行 (4 protected assets 全 untouched)

- [ ] **Step 9: 验证 8/8 验收**

```bash
# 1. reference/scene-master-list.md 存在 + 8 列 + V1 5 章示例
# 2. scripts/scan-scene-master-list.cjs 可跑
bun test scripts/scan-scene-master-list.test.ts 2>&1 | tail -5  # 期望 6/6 pass
# 3. baseline 报告 80 行
wc -l workspace/qi-shou-fan-shen-cheng-ding-fu/.agent/plan/i137-p2-5-baseline-report.md
# 4. templates/README.md 7 sections
grep -c "^## §" assets/workspace/.nbook/templates/README.md
# 5. 8 entity README 全存在
for e in chapter character faction item location note rule volume; do
    [ -f "assets/workspace/.nbook/templates/content-node-templates/$e/README.md" ] && echo "$e OK" || echo "$e MISSING"
done
# 6. relationship 模板存在
ls assets/workspace/.nbook/templates/content-node-templates/relationship/ 2>&1
# 7. main HEAD 0 改动 (archive 模式不变量)
git log main --oneline -1
# 8. 4 protected assets + 现有测试不退化
git log main..HEAD --stat | grep -E "(writer\.profile|neuro-agent-harness|server/agent/lore/|llmlint\.config)"
# 期望 0 命中
```

期望: 8/8 全 pass, 0 错误

---

## Success Criteria (End of Plan)

- ✅ 14 文件新, 0 文件改
- ✅ 9 commits on `feat-p2-5-p2-6` branch
- ✅ main HEAD = `dae6efb9` 0 改动 (除 spec commit)
- ✅ 0 push / 0 merge / 0 force-push
- ✅ 4 protected assets 全 untouched
- ✅ 8/8 spec §5 验收全 pass
- ✅ 14 文件 cp 主工作区 untracked (archive 模式)
- ✅ 调研报告进度 4/6 (67%) → 6/6 (100%) 完整锁版
- ✅ memory 同步: p2-5-p2-6-archive-closed-2026-08-20.md
- ✅ worktree 处置待用户拍板 (A 保留 / B 删除 + branch 保留, 沿用 P1-3 / I-1 / P1-4 / A1 先例)

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-20-p2-5-p2-6.md`. Two execution options:

1. **Subagent-Driven (this session)** — I dispatch fresh subagent per task, review between tasks, fast iteration
2. **Parallel Session (separate)** — Open new session with executing-plans, batch execution with checkpoints

Which approach?
